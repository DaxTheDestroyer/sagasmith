# Plan: Capture Phase 8 Context

## Goal
Run the standard `discuss-phase` workflow for Phase 8, then create `.planning/phases/08-retcon-repair-and-release-hardening/08-CONTEXT.md` with implementation decisions clear enough for downstream researcher and planner agents.

Plan mode is active now, so this plan does not write the context artifact. The only write performed in plan mode is this plan file.

## Workflow Mode
- `workflow.discuss_mode` resolved to `discuss`.
- Execute `.kilo/get-shit-done/workflows/discuss-phase.md`, not assumptions mode.
- `gsd-sdk query init.phase-op "8"` is unavailable in this environment, so use direct artifact inspection as a fallback for phase metadata and existing-file checks.

## Loaded Context
- Phase 8 from `.planning/ROADMAP.md`: `Retcon, Repair, and Release Hardening`.
- Phase 8 goal: user can safely retcon the last completed turn and the MVP is protected by full smoke/release gates.
- Phase 8 requirements: `QA-01`, `QA-02`, `QA-08`, `QA-09`.
- Phase 8 success criteria:
  - Retcon last completed turn after confirmation; retconned turns excluded from canonical replay, summaries, and vault rebuilds.
  - No-paid-call smoke suite proves install, init, configure, onboard, play skill challenge, play simple combat, quit, resume.
  - Release blocked unless lint, type check, unit tests, smoke tests, and secret scan pass.
- No `.planning/phases/08-*` directory exists yet.
- No existing Phase 8 context exists.
- Prior context found and reviewed: Phase 2, Phase 6, Phase 7.

## Prior Decisions To Carry Forward
- Default tests, smoke checks, and CI must make no paid calls; live provider checks are opt-in only.
- Secret leakage gates fail closed; API keys and auth headers must never appear in logs, vaults, transcripts, checkpoints, or generated artifacts.
- Runtime/checkpoint boundaries own persistence and recovery; agent nodes stay pure and return state updates only.
- `pre_narration` checkpoint recovery and discard flows already exist from Phase 6 and should inform retcon design.
- Phase 7 keeps vault as source of truth, player vault spoiler-safe, and derived layers rebuildable.
- Phase 7 deferred full `canon-conflict-detection` classifier and `callback-reachability-query`; do not pull those into Phase 8 unless user explicitly wants to note them as deferred.

## Codebase Scout Summary
Relevant implementation surfaces discovered by the explore agent:
- `src/sagasmith/tui/commands/control.py` has `RetconCommand` Phase 8 stub behavior.
- `src/sagasmith/graph/runtime.py` has existing `discard_incomplete_turn()`, `retry_narration()`, and `_rewind_to_checkpoint()` patterns.
- `src/sagasmith/persistence/repositories.py` has `TurnRecordRepository.get/upsert`; likely needs latest-completed helper.
- `src/sagasmith/schemas/persistence.py` and migration `0006_turn_record_status.sql` define current turn statuses; Phase 8 may need `retconned` status.
- `src/sagasmith/persistence/turn_close.py` is turn-close source of truth.
- `src/sagasmith/agents/archivist/transcript_context.py` joins `turn_records` and may need canonical-status filtering.
- `src/sagasmith/memory/fts5.py`, `src/sagasmith/memory/graph.py`, and `src/sagasmith/vault/__init__.py` are derived-layer and player-vault repair/rebuild surfaces.
- `src/sagasmith/evals/harness.py`, `src/sagasmith/cli/smoke_cmd.py`, `Makefile`, `.pre-commit-config.yaml`, and `.gitleaks.toml` are release/smoke-gate surfaces.
- Existing tests to reuse include `tests/integration/test_narration_recovery.py`, `tests/graph/test_checkpoints.py`, `tests/integration/test_tui_graph_smoke.py`, `tests/integration/test_rules_first_vertical_slice.py`, and `tests/evals/test_smoke_cli.py`.

## Canonical References For Phase 8 Context
Include these in `08-CONTEXT.md` unless discussion reveals additional user-specified docs:
- `.planning/ROADMAP.md` — Phase 8 goal, requirements, success criteria.
- `.planning/REQUIREMENTS.md` — `QA-01`, `QA-02`, `QA-08`, `QA-09` full requirement text.
- `.planning/PROJECT.md` — trust-before-breadth, deterministic services, local-first, secret safety, out-of-scope boundaries.
- `.planning/STATE.md` — current progress and deferred integration/release items.
- `.planning/phases/06-ai-gm-story-loop/06-CONTEXT.md` — narration recovery, deterministic stability, and Phase 8 retcon deferral.
- `.planning/phases/07-memory-vault-and-resume/07-CONTEXT.md` — vault/rebuild/player-vault sync decisions and Phase 8 deferrals.
- `docs/specs/GAME_SPEC.md` — game loop, turn flow, safety, and player-facing behavior.
- `docs/specs/PERSISTENCE_SPEC.md` — checkpoints, turn-close, repair, rebuild, atomicity.
- `docs/specs/STATE_SCHEMA.md` — persisted state, turn/checkpoint/delta contracts.
- `docs/specs/VAULT_SCHEMA.md` — canon/player vault projection and rebuild implications.
- `docs/specs/PF2E_MVP_SUBSET.md` — smoke flow mechanics boundaries for skill challenge/combat.
- `docs/specs/LLM_PROVIDER_SPEC.md` — no-paid-call, provider/fake-provider, cost, and secrets constraints.

## Preliminary Gray Areas To Present
Present 3-4 concrete gray areas and let the user choose which to discuss:

1. Retcon Confirmation Flow
   - How `/retcon` should ask for confirmation and how explicit the user action must be.
   - Candidate directions: two-step command, modal/prompt flow, typed confirmation.

2. Retcon Data Semantics
   - What “retcon the last completed turn” means in storage and audit terms.
   - Candidate directions: mark turn `retconned` and keep audit rows, inverse state deltas, checkpoint rewind plus derived rebuild.

3. Canonical Exclusion And Repair Behavior
   - How retconned turns are excluded from replay, summaries, memory packets, player vault rebuilds, and repair commands.
   - Candidate directions: status filter everywhere, canonical view/helper, rebuild-from-non-retconned source only.

4. MVP Smoke And Release Gate Shape
   - Whether QA-08/QA-09 should be a fast in-process smoke, shell-level `uv run sagasmith ...` smoke, or layered release gate.
   - Candidate directions: `make release-gate`, `sagasmith smoke --mode mvp`, CI/pre-commit-compatible command sequence.

## Execution Steps After Plan Mode
1. Re-check for existing Phase 8 directory, `08-CONTEXT.md`, `08-SPEC.md`, and interrupted `08-DISCUSS-CHECKPOINT.json`.
2. If Phase 8 context already exists by then, ask whether to update, view, or skip per workflow.
3. Check for relevant pending todos if the GSD tooling becomes available; otherwise note that the query tool was unavailable and proceed without folded todos.
4. Present the Phase 8 boundary and carried-forward prior decisions.
5. Ask the user which preliminary gray areas to discuss using the `question` tool.
6. For each selected area, conduct focused question loops; avoid scope creep and record deferred ideas.
7. After each area completes, write/update `08-DISCUSS-CHECKPOINT.json` during execution mode.
8. Create `.planning/phases/08-retcon-repair-and-release-hardening/08-CONTEXT.md` using the required template sections: domain, decisions, canonical refs, code context, specifics, deferred.
9. Create `.planning/phases/08-retcon-repair-and-release-hardening/08-DISCUSSION-LOG.md` as the audit trail.
10. Remove the checkpoint file after successful context creation.
11. If GSD commit/state commands are available, use them as the workflow requires; if still unavailable, report the created files and next command without mutating state through unsupported tooling.

## Output Files
- Primary: `.planning/phases/08-retcon-repair-and-release-hardening/08-CONTEXT.md`
- Audit log: `.planning/phases/08-retcon-repair-and-release-hardening/08-DISCUSSION-LOG.md`
- Temporary checkpoint during discussion: `.planning/phases/08-retcon-repair-and-release-hardening/08-DISCUSS-CHECKPOINT.json`

## Completion Criteria
- Prior context applied; no already-decided questions re-asked.
- User-selected gray areas discussed to satisfaction.
- Scope creep recorded only as deferred ideas.
- `08-CONTEXT.md` contains concrete decisions, mandatory canonical refs, and code context.
- User receives next step: `/gsd-plan-phase 8`.
