# Phase 4 Replan (post `/gsd-review`) — Plan Summary

**Goal:** Replan Phase 4 ("Graph Runtime and Agent Skills") incorporating all HIGH/MEDIUM feedback from `.planning/phases/04-graph-runtime-and-agent-skills/04-REVIEWS.md` (Gemini, Claude, Codex, OpenCode).

## Scope

- Replace the original three-plan structure (04-01, 04-02, 04-03) with a five-plan structure that splits the two oversized plans and inserts an end-to-end TUI→graph smoke test.
- Preserve phase requirements coverage: GRAPH-01..05, AI-12, SKILL-01..05.
- Do not change roadmap phase boundaries or downstream phase scope.

## Structure

| Wave | Plans | Builds |
|------|-------|--------|
| 1 | 04-01 | LangGraph deps + typed `SagaGraphState` + five thin node stubs + `pending_narration` on `SagaState` + combat-phase deferral note |
| 2 | 04-02 ∥ 04-04 | **04-02** LangGraph spike + SqliteSaver runtime + activation log (GRAPH-02/03/05, AI-12). **04-04** Skills adapter package + frontmatter parser + `pyproject` packaging (SKILL-01/02/03). Parallel because zero `files_modified` overlap. |
| 3 | 04-03 | Native LangGraph `interrupt()` / `Command(resume=...)` for pause/line/budget_stop/session_end + `/pause` + `/line` TUI wiring + E2E smoke test (GRAPH-04) |
| 4 | 04-05 | 14 first-slice `SKILL.md` files + contextvar `set_skill` wiring + `first_slice_only=True` E2E proof (SKILL-04, SKILL-05) |

## Review Feedback Integrated

### HIGH-severity

1. Replace shadow `pending_interrupt` state field with LangGraph native `interrupt()` / `Command(resume=...)` (Claude, Codex, OpenCode).
2. Task split: original two-task plans expanded to 2–3 tasks across five plans (Claude, Codex).
3. Pre-narration `CheckpointRef` owned by a single `GraphRuntime._record_pre_narration_checkpoint` method, not arbitrary callers (OpenCode, Claude).
4. Thread-id convention locked to `campaign:<id>` (Option A); `turn_id` lives in state + `checkpoint_refs` (Codex).
5. Checkpoint-id access proven by a LangGraph spike test BEFORE production code commits (Codex).
6. `SKILL.md` packaging via `pyproject.toml` `[tool.hatch|setuptools]` package data + installed-style `importlib.resources` test (Codex).
7. `first_slice` is a first-class field on `SkillRecord` and frontmatter (default `True`) (Claude, OpenCode).

### MEDIUM-severity

8. `BudgetStopError` → interrupt translation lives in the runtime wrapper, not node bodies (thin-node invariant preserved) (Codex).
9. `pending_narration` schema change hoisted to 04-01 Task 1 with explicit fixture + JSON-schema export sweep (Claude, OpenCode).
10. `RetconCommand` is acknowledge-only in Phase 4 (no interrupt posted); Phase 8 owns retcon flow (Claude).
11. Reject `allowed_agents: ["*"]` on agent-scoped skills; no silent downgrade (Claude).
12. Deterministic `sorted(Path.rglob())` scan order so catalogs are OS-stable (Codex).
13. Replace brittle `== 14` exact-count assertions with required-set containment plus `len(store.errors) == 0` (Codex).
14. Isolate YAML-lite frontmatter parser into its own module with documented `SUPPORTED_SUBSET` constant (Gemini, Codex, OpenCode).
15. `_default_skill_store()` raises loudly on production scan errors rather than returning silently (OpenCode).
16. Add SqliteSaver table-name collision test against SagaSmith migrations (Gemini, Claude, OpenCode).

### Cross-cutting

17. Add end-to-end TUI input → graph turn → stub narration smoke test at `tests/integration/test_tui_graph_smoke.py` (Claude, OpenCode).

## File-Overlap Check (within each wave)

- Wave 1: 04-01 alone.
- Wave 2 (parallel): 04-02 owns `src/sagasmith/graph/*` + `src/sagasmith/persistence/*`. 04-04 owns `src/sagasmith/skills_adapter/*` + `pyproject.toml`. 04-01 already released `pyproject.toml` in Wave 1 so 04-04 re-editing it does not race with 04-02.
- Wave 3: 04-03 alone.
- Wave 4: 04-05 alone.

## Out of Scope (explicit deferrals)

- Combat-phase sub-routing and full `CombatState` payload handling — deferred to Phase 5.
- Retcon rollback and confirmation flow — deferred to Phase 8.
- Phase 6 real token/cost estimates for `precheck_estimated` — Phase 4 uses zero-estimate hook only inside the runtime wrapper.
- `memory-packet-assembly` handler implementation — Phase 7.
- Prompted-skill body consumption by LLM system prompts — Phase 6 (Phase 4 ships bodies as metadata-only for prompted skills).
- Non-first-slice skills per `docs/specs/agents/*.md §3` — listed in `04-05-SUMMARY.md` with target phase.

## Artifacts Already Written by the Orchestrator

- `.planning/phases/04-graph-runtime-and-agent-skills/04-01-PLAN.md` (revised)
- `.planning/phases/04-graph-runtime-and-agent-skills/04-02-PLAN.md` (revised, formerly persistence+interrupts+skills; now checkpoints + activation log only)
- `.planning/phases/04-graph-runtime-and-agent-skills/04-03-PLAN.md` (new scope: native interrupts + TUI wiring + E2E smoke)
- `.planning/phases/04-graph-runtime-and-agent-skills/04-04-PLAN.md` (new: skills adapter)
- `.planning/phases/04-graph-runtime-and-agent-skills/04-05-PLAN.md` (new: 14 SKILL.md + wiring)
- `.planning/ROADMAP.md` plan count for Phase 4 bumped from 3 → 5.
- Commit recorded via the planner subagent.

## Verification Loop Result

Planner + plan-checker passed in one iteration: coverage complete for GRAPH-01..05, AI-12, SKILL-01..05; all review concerns traced to specific plan tasks; no file overlap between same-wave plans.

## Next Step (for user, after plan acceptance)

```
/gsd-execute-phase 4
```

If `auto_advance: true` is retained in `.planning/config.json`, `/gsd-execute-phase 4 --auto` will chain through the four waves.
