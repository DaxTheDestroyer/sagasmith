"""Agent prompts and adapters for SagaSmith runtime roles.

Directory layout follows ADR-0001:
  agents/<name>/node.py    — thin LangGraph node callable
  agents/<name>/skills/    — per-agent SKILL.md directories (Plan 04-05)
"""
