---
phase: 07-memory-vault-and-resume
plan: 06
type: execute
wave: 6
depends_on:
  - "07-05"
files_modified:
  - tests/vault/test_player_projection.py
  - tests/integration/test_quit_resume_flow.py
  - tests/integration/test_vault_repair.py
  - tests/cli/test_vault_cli.py
  - tests/agents/archivist/test_end_to_end_memory.py
  - .planning/phases/07-memory-vault-and-resume/07-ALL-SUMMARY.md (aggregated)
autonomous: true
requirements:
  - QA-06
  - QA-08 (partial: smoke)
  - QA-09 (partial: release gate validation)

must_haves:
  truths:
    - "Player-vault projection contains zero GM-only frontmatter fields, secrets, or inline GM comments across all page types."
    - "A full quit/resume cycle preserves campaign state: vault paths, rolling summary, memory indices, and session number increments correctly."
    - "Vault repair commands (`rebuild`, `sync`) restore derived indices and player vault from a corrupted state."
    - "All lint, typecheck, and unit tests pass; the project meets the Phase 7 release gate."
  artifacts:
    - path: tests/vault/test_player_projection.py
      provides: "GM-leakage regression test scanning player vault files for forbidden patterns"
    - path: tests/integration/test_quit_resume_flow.py
      provides: "End-to-end test simulating play, quit, and resume with assertions on vault state"
    - path: tests/cli/test_vault_cli.py
      provides: "CLI tests for vault rebuild/sync commands"
    - path: tests/agents/archivist/test_end_to_end_memory.py
      provides: "Cross-skill integration: memory packet uses FTS5+NetworkX, token cap holds, entity resolution works"
  key_links:
    - from: tests/vault/test_player_projection.py
      to: src/sagasmith/vault/writer.py
      via: "sync() call produces player vault files, then scan for 'gm_' patterns"
      pattern: "gm_"
    - from: integration test
      to: src/sagasmith/graph/runtime.py
      via: "GraphRuntime.invoke_turn and resume_and_close simulate full turn"
      pattern: "invoke_turn"

---

<objective>
Finalize Phase 7 with comprehensive regression, integration, and QA tests, then run the full quality gate (lint/typecheck/unit/smoke). This plan ensures the release criteria for the memory/vault/resume milestone are objectively met.

**Purpose:** Provide confidence that every requirement (CLI-04, PERS-03/05/06, VAULT-01–10, AI-11, TUI-08, QA-06) is covered by automated tests and the codebase passes all quality checks.

**Output:** Test suite with ≥ 95% coverage on Archivist modules, full pipeline green, and SUMMARY documenting what was verified.
</objective>

<execution_context>
@.kilo/get-shit-done/workflows/execute-plan.md
@.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@docs/specs/VAULT_SCHEMA.md
@docs/specs/PERSISTENCE_SPEC.md
@.planning/phases/07-memory-vault-and-resume/07-05-PLAN.md
</context>

<interfaces>
We consume all previously implemented services:
- VaultService, FTS5Index, NetworkX graph, Archivist node, MemoryPacket, etc.
We add test-only fixtures from tests.fixtures.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write GM-leakage regression test (QA-06)</name>
  <files>
    tests/vault/test_player_projection.py
  </files>
  <action>
    - Setup: create a temporary master vault with pages containing every GM-only pattern:
      * frontmatter fields: `secrets:`, `gm_notes:`
      * body with `<!-- gm: secret note -->` and multiline `<!-- gm:\n  line1\n  line2\n-->`
      * visibility `gm_only` page present
    - Run VaultService.sync() to player vault.
    - Recursively scan player vault directory for any occurrence of `gm_`, `secrets:`, `"<!-- gm:` patterns.
    - Assert zero matches; also assert that foreshadowed stubs contain only "*Unknown - you have heard this name but know little more.*".
    - Include parameterized checks for each forbidden pattern.
  </action>
  <verify>
    - `pytest tests/vault/test_player_projection.py -x`
  </verify>
  <done>QA-06 test ensuring no GM leakage passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write end-to-end quit/resume integration test</name>
  <files>
    tests/integration/test_quit_resume_flow.py
  </files>
  <action>
    - Use `DeterministicFakeClient` for no-paid-call flow.
    - Steps:
      1. Initialize campaign, run onboarding.
      2. Play 3 turns through graph runtime with fake provider (mock LLM returning canned responses).
      3. After third turn, trigger `SESSION_END` interrupt via `runtime.post_interrupt(kind=SESSION_END)`.
      4. Close app; verify that player vault sync occurred (player_vault directory exists and contains expected pages).
      5. Re-open campaign via `build_app` with same root.
      6. Verify session_id increments to "session_002".
      7. Verify graph state contains rolling_summary from previous session.
      8. Start turn 4; ensure MemoryPacket contains expected entities from earlier sessions (via FTS5 retrieval) and respects token cap.
    - Use SQLite queries to confirm turn_records.status='complete', no `sync_warning`.
    - Also test resume after interrupted turn (pre-narration) by leaving orphan checkpoint, then restart and verify retry path (could reuse Phase 6 tests).
  </action>
  <verify>
    - `pytest tests/integration/test_quit_resume_flow.py -x`
    - Test runs under 60 seconds.
  </verify>
  <done>Quit/resume lifecycle validated end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Final quality gate and documentation</name>
  <files>
    .planning/phases/07-memory-vault-and-resume/07-ALL-SUMMARY.md
  </files>
  <action>
    1. Run full lint: `uv run ruff check .` → fix any findings.
    2. Run typecheck: `uv run pyright` → zero errors.
    3. Run unit + integration tests: `pytest -x` → all pass.
    4. Run smoke suite: `uv run sagasmith smoke --campaign <fixture>` → passes.
    5. Run secret scan: `gitleaks detect --no-git` or equivalent → clean.
    6. Aggregate coverage: ensure critical paths (vault write, sync, memory assembly) have ≥ 90% line coverage.
    7. Write `07-ALL-SUMMARY.md` summarizing all delivered artifacts, open risks (LanceDB stub, callback-reachability deferred), and next steps (Phase 8).
  </action>
  <verify>
    - CI-equivalent local run: all commands succeed.
    - `git diff --stat` shows expected file changes matching plan files_modified.
  </verify>
  <done>All quality gates passed; phase ready for handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → production vault | Tests create temporary vaults; ensure cleanup to avoid leaking test data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-19 | Tampering | Test intentionally corrupts vault to check repair; could leave repo dirty | mitigate | Tests use temporary directories; teardown removes all created files with `shutil.rmtree`. |
| T-07-20 | Information Disclosure | Test fixtures contain example secrets; ensure fixtures are not committed with real keys | accept | All fixtures use fake data; repo scanned for secrets. |
</threat_model>

<verification>
- Run `uv run ruff check .`
- Run `uv run pyright`
- Run `pytest -x`
- Run `uv run sagasmith smoke`
- Run secret scan `gitleaks detect --no-git`
</verification>

<success_criteria>
- [ ] QA-06: Player-vault projection test passes (no GM content leaks).
- [ ] QA-08 resistance: Smoke suite covering install/init/onboard/play skill challenge/play combat/quit/resume all passes without paid LLM calls (Phase 8 final gate prerequisite).
- [ ] QA-09: All quality gates pass locally, matching CI expectations.
- [ ] All Phase 7 requirement IDs are satisfied by implemented features and tests.
- [ ] Phase summary document (07-ALL-SUMMARY.md) accurately reflects completed work and remaining gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/07-memory-vault-and-resume/07-ALL-SUMMARY.md` (aggregated phase summary)
</output>
