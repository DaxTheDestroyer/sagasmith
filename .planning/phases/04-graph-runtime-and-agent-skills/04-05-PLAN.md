---
phase: 04-graph-runtime-and-agent-skills
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-04"]
files_modified:
  - src/sagasmith/skills/__init__.py
  - src/sagasmith/skills/schema-validation/SKILL.md
  - src/sagasmith/skills/safety-redline-check/SKILL.md
  - src/sagasmith/skills/command-dispatch/SKILL.md
  - src/sagasmith/agents/oracle/skills/scene-brief-composition/SKILL.md
  - src/sagasmith/agents/oracle/skills/player-choice-branching/SKILL.md
  - src/sagasmith/agents/oracle/skills/content-policy-routing/SKILL.md
  - src/sagasmith/agents/oracle/skills/inline-npc-creation/SKILL.md
  - src/sagasmith/agents/rules_lawyer/skills/degree-of-success/SKILL.md
  - src/sagasmith/agents/rules_lawyer/skills/seeded-roll-resolution/SKILL.md
  - src/sagasmith/agents/rules_lawyer/skills/skill-check-resolution/SKILL.md
  - src/sagasmith/agents/orator/skills/scene-rendering/SKILL.md
  - src/sagasmith/agents/archivist/skills/memory-packet-assembly/SKILL.md
  - src/sagasmith/agents/archivist/skills/turn-close-persistence/SKILL.md
  - src/sagasmith/agents/onboarding/skills/onboarding-phase-wizard/SKILL.md
  - src/sagasmith/graph/bootstrap.py
  - src/sagasmith/graph/__init__.py
  - src/sagasmith/agents/oracle/node.py
  - src/sagasmith/agents/rules_lawyer/node.py
  - src/sagasmith/agents/orator/node.py
  - src/sagasmith/agents/archivist/node.py
  - src/sagasmith/agents/onboarding/node.py
  - tests/agents/test_nodes_with_skills.py
  - tests/skills_adapter/test_production_catalog.py
autonomous: true
requirements:
  - SKILL-04
  - SKILL-05
revised_for_reviews: true
review_issues_addressed:
  - "[04-03 HIGH → 04-05] Task 2 split: this plan isolates SKILL.md shipping + node wiring + activation `set_skill` calls (Claude, Codex)."
  - "[04-03 LOW → 04-05] Skill count assertions use required-set containment instead of exact `== 14` (Codex)."
  - "[04-03 MEDIUM → 04-05] SkillStore.errors surfaced: bootstrap helper logs + raises on first-slice startup errors so developers see them (OpenCode)."
  - "[04-03 MEDIUM → 04-05] Onboarding skill rationale documented (doc refers to the 9-phase wizard already shipped in Plan 03-02) (Claude)."
  - "[04-03 LOW → 04-05] memory-packet-assembly flagged `first_slice: false` with target phase 7 (Claude)."
  - "[04-03 LOW → 04-05] Deferred skills explicitly listed in SUMMARY with target phases (Claude)."
must_haves:
  truths:
    - "Every first-slice SKILL.md listed in docs/specs/agents/*.md §3 is shipped in src/sagasmith/{agents|skills}/ with valid frontmatter."
    - "SkillStore scanned against the production directories returns zero `errors` and a required-set of first-slice skills covering all four agents + the three cross-cutting skills."
    - "Each agent node records its active skill via `get_current_activation().set_skill(...)` through the contextvar handoff exposed by Plan 04-02."
    - "Deterministic skills (degree-of-success, seeded-roll-resolution, skill-check-resolution, turn-close-persistence) have their handlers referenced from the node code or from the runtime wrapper (no prompted LLM call required to execute them)."
    - "A `first_slice_only=True` SkillStore + the persistent graph runtime still completes a full play turn end-to-end."
  artifacts:
    - path: "src/sagasmith/skills/schema-validation/SKILL.md"
      provides: "Cross-cutting: JSON Schema validation (services-capabilities.md §2.6)"
    - path: "src/sagasmith/skills/safety-redline-check/SKILL.md"
      provides: "Cross-cutting: player-visible artifact redline scan (§2.7)"
    - path: "src/sagasmith/skills/command-dispatch/SKILL.md"
      provides: "Cross-cutting: slash-command parsing rules reference (§2.9)"
    - path: "src/sagasmith/agents/oracle/skills/*/SKILL.md"
      provides: "4 Oracle first-slice skills per oracle-skills.md §3"
    - path: "src/sagasmith/agents/rules_lawyer/skills/*/SKILL.md"
      provides: "3 RulesLawyer first-slice skills per rules-lawyer-skills.md §3 (deterministic)"
    - path: "src/sagasmith/agents/orator/skills/scene-rendering/SKILL.md"
      provides: "Orator first-slice rendering skill (stubbed for Phase 6)"
    - path: "src/sagasmith/agents/archivist/skills/*/SKILL.md"
      provides: "2 Archivist skills: turn-close-persistence (first_slice=true, deterministic) + memory-packet-assembly (first_slice=false, Phase 7)"
    - path: "src/sagasmith/agents/onboarding/skills/onboarding-phase-wizard/SKILL.md"
      provides: "Onboarding skill documenting the 9-phase wizard shipped in Plan 03-02"
  key_links:
    - from: "src/sagasmith/agents/rules_lawyer/node.py"
      to: "src/sagasmith/skills_adapter/loader.py::load_skill"
      via: "rules_lawyer_node calls load_skill('skill-check-resolution', agent_name='rules_lawyer') when trigger matches + records set_skill on contextvar"
      pattern: "load_skill|set_skill"
    - from: "src/sagasmith/agents/*/node.py"
      to: "src/sagasmith/graph/activation_log.py::get_current_activation"
      via: "every node calls get_current_activation().set_skill(<skill-name>)"
      pattern: "get_current_activation"
    - from: "src/sagasmith/graph/bootstrap.py::GraphBootstrap.from_services"
      to: "src/sagasmith/skills_adapter/store.py::SkillStore"
      via: "default SkillStore points at src/sagasmith/agents + src/sagasmith/skills production roots"
      pattern: "SkillStore|_default_skill_store"
---

<objective>
Ship the first-slice Agent Skills catalog: 14 valid SKILL.md files across 5 agents + 3 cross-cutting skills, wire every agent node to record its active skill via the contextvar handoff from Plan 04-02, and prove an end-to-end play turn with `first_slice_only=True` completes. Delivers SKILL-04 (activation logging with skill_name) and the SKILL-05 end-to-end proof.

Purpose: The adapter from Plan 04-04 is dormant without shipped skills. This plan ships the catalog documented in the agent specs, wires node-side `set_skill` calls, and adds a startup-time validation that catches SKILL.md errors before the graph runs.
Output: 14 SKILL.md files in production directories, per-node `set_skill` wiring, bootstrap validation of the production skill store, full Phase 4 regression green including a `first_slice_only=True` end-to-end play turn.

## Revision notes (from 04-REVIEWS.md)

- **Plan split**: this plan was extracted from the original 04-03 Task 2 per consensus task-split feedback.
- **Required-set assertions**: instead of `== 14`, tests assert that the store contains a required set of first-slice skill names (by agent) with 0 errors. Adding skills later doesn't break tests.
- **Bootstrap surface errors**: `_default_skill_store()` scans on construction and raises if `store.errors` is non-empty — so developers see SKILL.md problems at import time, not silently at first turn.
- **Onboarding skill documented**: the SKILL.md explicitly references the 9-phase wizard already delivered in Plan 03-02.
- **Memory-packet-assembly marked `first_slice: false`** and its SKILL.md body states Phase 7 owns implementation.
- **Deferred skills listed**: SUMMARY enumerates every non-shipped spec skill with target phase to prevent drift.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-graph-runtime-and-agent-skills/04-REVIEWS.md
@.planning/phases/04-graph-runtime-and-agent-skills/04-01-SUMMARY.md
@.planning/phases/04-graph-runtime-and-agent-skills/04-02-SUMMARY.md
@.planning/phases/04-graph-runtime-and-agent-skills/04-04-SUMMARY.md
@docs/specs/ADR-0001-orchestration-and-skills.md
@docs/specs/agents/README.md
@docs/specs/agents/oracle-skills.md
@docs/specs/agents/rules-lawyer-skills.md
@docs/specs/agents/archivist-skills.md
@docs/specs/agents/services-capabilities.md
@src/sagasmith/graph/bootstrap.py
@src/sagasmith/graph/activation_log.py
@src/sagasmith/graph/runtime.py
@src/sagasmith/agents/oracle/node.py
@src/sagasmith/agents/rules_lawyer/node.py
@src/sagasmith/agents/orator/node.py
@src/sagasmith/agents/archivist/node.py
@src/sagasmith/agents/onboarding/node.py
@src/sagasmith/services/pf2e.py
@src/sagasmith/services/dice.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship 14 first-slice SKILL.md files + bootstrap helper that scans and fails loud on errors</name>
  <files>
    src/sagasmith/skills/__init__.py,
    src/sagasmith/skills/schema-validation/SKILL.md,
    src/sagasmith/skills/safety-redline-check/SKILL.md,
    src/sagasmith/skills/command-dispatch/SKILL.md,
    src/sagasmith/agents/oracle/skills/scene-brief-composition/SKILL.md,
    src/sagasmith/agents/oracle/skills/player-choice-branching/SKILL.md,
    src/sagasmith/agents/oracle/skills/content-policy-routing/SKILL.md,
    src/sagasmith/agents/oracle/skills/inline-npc-creation/SKILL.md,
    src/sagasmith/agents/rules_lawyer/skills/degree-of-success/SKILL.md,
    src/sagasmith/agents/rules_lawyer/skills/seeded-roll-resolution/SKILL.md,
    src/sagasmith/agents/rules_lawyer/skills/skill-check-resolution/SKILL.md,
    src/sagasmith/agents/orator/skills/scene-rendering/SKILL.md,
    src/sagasmith/agents/archivist/skills/memory-packet-assembly/SKILL.md,
    src/sagasmith/agents/archivist/skills/turn-close-persistence/SKILL.md,
    src/sagasmith/agents/onboarding/skills/onboarding-phase-wizard/SKILL.md,
    src/sagasmith/graph/bootstrap.py,
    tests/skills_adapter/test_production_catalog.py
  </files>
  <read_first>
    docs/specs/agents/oracle-skills.md,
    docs/specs/agents/rules-lawyer-skills.md,
    docs/specs/agents/archivist-skills.md,
    docs/specs/agents/services-capabilities.md,
    docs/specs/agents/README.md,
    src/sagasmith/skills_adapter/store.py,
    src/sagasmith/skills_adapter/loader.py,
    src/sagasmith/graph/bootstrap.py
  </read_first>
  <behavior>
    - Test 1 (production scan clean): `SkillStore(roots=[Path('src/sagasmith/agents'), Path('src/sagasmith/skills')]).scan()` returns `store.errors == []`.
    - Test 2 (required-set coverage, first-slice): the scanned store contains at minimum these skill names by scope:
      - `_shared`: `schema-validation`, `safety-redline-check`, `command-dispatch`
      - `oracle`: `scene-brief-composition`, `player-choice-branching`, `content-policy-routing`, `inline-npc-creation`
      - `rules_lawyer`: `degree-of-success`, `seeded-roll-resolution`, `skill-check-resolution`
      - `orator`: `scene-rendering`
      - `archivist`: `turn-close-persistence` (memory-packet-assembly may exist but is not first_slice)
      - `onboarding`: `onboarding-phase-wizard`
      Assert: for each (scope, required_name) in the required set, `store.find(name=required_name, agent_scope=scope) is not None`. Count is derived, not hard-coded.
    - Test 3 (first_slice filtering): `SkillStore(roots=..., first_slice_only=True).scan()` excludes `memory-packet-assembly` (the only first_slice=false skill shipped) and keeps all others. `store.skipped` has one entry with reason `"first_slice_only filter"`.
    - Test 4 (implementation_surface matches spec): for every shipped skill, assert `implementation_surface` matches the catalog in the corresponding spec file (deterministic for rules_lawyer trio + turn-close-persistence + schema-validation + safety-redline-check + command-dispatch + onboarding-phase-wizard; prompted/hybrid for Oracle + Orator + memory-packet-assembly per spec).
    - Test 5 (bootstrap_loud_on_error): `_default_skill_store()` returns a scanned store; if `store.errors` is non-empty at construction, the helper raises `SkillValidationError` with a joined list of errors. Test by temporarily patching one production SKILL.md to invalid (monkeypatch); assert `_default_skill_store()` raises.
    - Test 6 (name-directory match): every shipped skill's `name` frontmatter matches its directory name exactly (lexical equality). Asserted by iterating `store.skills.values()` and comparing.
    - Test 7 (description length): every description ≤ 256 chars (already enforced by store.scan, but this is a specification-contract test).
    - Test 8 (lightweight bootstrap): importing `sagasmith.graph.bootstrap` runs `_default_skill_store()` lazily (on `AgentServices(...)` construction) NOT at module import, so test fixtures that don't need skills still import cheaply.
  </behavior>
  <action>
    **Write 14 SKILL.md files** following the exact frontmatter schema from Plan 04-04. Every file:
    - frontmatter opens/closes with `---`
    - `name` matches directory
    - `description` (≤ 256 chars, from spec catalog)
    - `allowed_agents`: `[<parent agent>]` for agent-scoped, `["*"]` for cross-cutting
    - `implementation_surface`: per spec
    - `first_slice: true` for all except `memory-packet-assembly` (false)
    - `success_signal`: from spec
    - body: three sections `## When to Activate`, `## Procedure`, `## Failure Handling`; for deterministic skills also `## Deterministic Handler` pointing to module + function.

    **Cross-cutting (`src/sagasmith/skills/`)**:

    `schema-validation/SKILL.md`:
    ```
    ---
    name: schema-validation
    description: Validate any dict or model payload against its Pydantic schema before persistence or LLM emission.
    allowed_agents: ["*"]
    implementation_surface: deterministic
    first_slice: true
    success_signal: Every persisted or LLM-boundary payload is validated; invalid payloads raise SchemaModel.ValidationError before any I/O.
    ---
    # Schema Validation

    ## When to Activate
    Before persisting any dict to SQLite, before emitting any payload to an LLM, or
    when consuming an LLM response that claims to be structured output.

    ## Procedure
    Use `pydantic.TypeAdapter(Model).validate_python(payload)` or
    `Model.model_validate(payload)`. On failure, raise immediately — never
    persist a partially-validated payload.

    ## Deterministic Handler
    Module: `sagasmith.schemas` (each model has `.model_validate` / `.model_dump`).
    Helper: `pydantic.TypeAdapter`.

    ## Failure Handling
    Log `ValidationError` with redacted details; do NOT retry without a fresh
    payload. Re-raise to the caller.
    ```

    `safety-redline-check/SKILL.md`:
    ```
    ---
    name: safety-redline-check
    description: Scan any player-visible or persisted payload for secret-shaped redline content before emission or write.
    allowed_agents: ["*"]
    implementation_surface: deterministic
    first_slice: true
    success_signal: No payload containing redline patterns reaches the player or the database.
    ---
    # Safety Redline Check

    ## When to Activate
    Before any write to campaign files, vaults, transcripts, checkpoints, safety
    events, or agent-skill-log rows. Also before any narration emission.

    ## Procedure
    Call `RedactionCanary().scan(payload_as_string)`. A return of True means
    redline content is present — reject the payload.

    ## Deterministic Handler
    Module: `sagasmith.evals.redaction`.
    Function: `RedactionCanary.scan`.

    ## Failure Handling
    Raise `TrustServiceError("redlined content detected")` without echoing the
    offending payload into logs or error messages.
    ```

    `command-dispatch/SKILL.md`:
    ```
    ---
    name: command-dispatch
    description: Parse and dispatch slash commands to registered handlers using the TUI CommandRegistry.
    allowed_agents: ["*"]
    implementation_surface: deterministic
    first_slice: true
    success_signal: Every supported slash command invokes its registered handler; unknown commands surface a friendly "unknown command" message.
    ---
    # Command Dispatch

    ## When to Activate
    When a player input starts with `/` in the Textual shell.

    ## Procedure
    The TUI layer (Plan 03-03) handles this via `CommandRegistry.dispatch(app, line)`.
    This skill exists as an audit reference.

    ## Deterministic Handler
    Module: `sagasmith.tui.commands.registry`.
    Class: `CommandRegistry`.

    ## Failure Handling
    Unknown commands emit a player-visible narration line and log nothing else.
    ```

    **Oracle (`src/sagasmith/agents/oracle/skills/`)**:

    `scene-brief-composition/SKILL.md` (prompted, first_slice):
    ```
    ---
    name: scene-brief-composition
    description: Build a structured SceneBrief for the next playable scene based on campaign state, memory packet, and player preferences.
    allowed_agents: [oracle]
    implementation_surface: prompted
    first_slice: true
    success_signal: Every brief includes required fields and never contains player-facing narration.
    ---
    # Scene Brief Composition

    ## When to Activate
    At the start of every turn where `state.phase == "play"` and `state.scene_brief`
    is None or stale.

    ## Procedure
    (Phase 6 implementation.) LLM synthesizes a SceneBrief using MemoryPacket,
    content_policy, and player_profile. Strict output format per oracle-skills.md §2.3.

    ## Failure Handling
    If the LLM response fails schema validation, retry once; on second failure,
    route to safety-redline-check and surface a safe fallback SceneBrief.
    ```

    `player-choice-branching/SKILL.md` (prompted, first_slice): description covers "Plan 2-3 branching affordances for the next player beat aligned with scene intent." Body references oracle-skills.md §2.4.

    `content-policy-routing/SKILL.md` (hybrid, first_slice): description covers "Route proposed scene elements through content_policy rules; reshape or reject content that conflicts with player lines." Body references oracle-skills.md §2.9.

    `inline-npc-creation/SKILL.md` (prompted, first_slice): description covers "Create a minimally-specified NPC inline when the scene demands one, respecting canon and locale conventions." Body references oracle-skills.md §2.7.

    **RulesLawyer (`src/sagasmith/agents/rules_lawyer/skills/`)**:

    `degree-of-success/SKILL.md` (deterministic, first_slice):
    ```
    ---
    name: degree-of-success
    description: Compute PF2e degree of success (critical success, success, failure, critical failure) from natural roll, total, and DC.
    allowed_agents: [rules_lawyer]
    implementation_surface: deterministic
    first_slice: true
    success_signal: Every check result has a correct degree per PF2e rules including natural 1/20 step adjustment.
    ---
    # Degree of Success

    ## When to Activate
    After any check roll is resolved by seeded-roll-resolution.

    ## Procedure
    Pass `natural`, `total`, `dc` to the deterministic handler.

    ## Deterministic Handler
    Module: `sagasmith.services.pf2e`.
    Function: `compute_degree(natural: int, total: int, dc: int) -> Literal["critical_success","success","failure","critical_failure"]`.

    ## Failure Handling
    Illegal inputs (non-integer natural outside 1-20) raise ValueError — DO NOT coerce.
    ```

    `seeded-roll-resolution/SKILL.md` (deterministic, first_slice): references `sagasmith.services.dice.DiceService.roll_d20`.

    `skill-check-resolution/SKILL.md` (deterministic, first_slice): composes the two above — calls DiceService then compute_degree. References rules-lawyer-skills.md §2.3.

    **Orator (`src/sagasmith/agents/orator/skills/`)**:

    `scene-rendering/SKILL.md` (prompted, first_slice): describes "First-slice narrative rendering for completed mechanical resolutions; respects dice UX visibility and never contradicts resolved degree-of-success." Notes Phase 6 wires streaming narration.

    **Archivist (`src/sagasmith/agents/archivist/skills/`)**:

    `memory-packet-assembly/SKILL.md` (**hybrid, `first_slice: false`**): body explicitly states Phase 7 owns implementation; lives in the catalog for completeness.

    `turn-close-persistence/SKILL.md` (deterministic, first_slice):
    ```
    ---
    name: turn-close-persistence
    description: Atomically persist turn records, roll logs, provider logs, state deltas, cost logs, and checkpoint refs within a single SQLite transaction.
    allowed_agents: [archivist]
    implementation_surface: deterministic
    first_slice: true
    success_signal: turn_records.status transitions to "complete" only after COMMIT; all related rows share the same turn_id.
    ---
    # Turn Close Persistence

    ## When to Activate
    At archivist completion of every play turn.

    ## Procedure
    Build a TurnCloseBundle; invoke the deterministic handler. Plan 04-02's
    GraphRuntime owns the actual invocation to keep nodes thin.

    ## Deterministic Handler
    Module: `sagasmith.persistence.turn_close`.
    Function: `close_turn(conn, bundle)`.

    ## Failure Handling
    Any exception inside the transaction rolls back all writes; turn_records.status
    remains "open". No partial persistence.
    ```

    **Onboarding (`src/sagasmith/agents/onboarding/skills/`)**:

    `onboarding-phase-wizard/SKILL.md` (deterministic, first_slice):
    ```
    ---
    name: onboarding-phase-wizard
    description: Drive the 9-phase onboarding wizard (story preferences, content policy, house rules, budget, dice UX, campaign length, character mode) to produce validated PlayerProfile, ContentPolicy, and HouseRules.
    allowed_agents: [onboarding]
    implementation_surface: deterministic
    first_slice: true
    success_signal: OnboardingStore.commit() produces a valid triple; re-runs are idempotent.
    ---
    # Onboarding Phase Wizard

    ## When to Activate
    When `state.player_profile`, `state.content_policy`, or `state.house_rules` is None.

    ## Procedure
    The wizard was delivered in Plan 03-02. This SKILL.md documents the existing
    state machine + SQLite-backed store; the onboarding node in the graph calls
    into `sagasmith.onboarding.store.OnboardingStore` to advance the wizard.

    ## Deterministic Handler
    Module: `sagasmith.onboarding.store`.
    Class: `OnboardingStore` (state machine + atomic commit).

    ## Failure Handling
    Validation errors surface on `.commit()`; partial state stays in the store
    until the user completes or abandons.
    ```

    **Create `src/sagasmith/skills/__init__.py`** with a short docstring stating the directory is a resource root, not a code package. (File exists for `importlib.resources` support per Plan 04-04 Test 19.)

    **Update `src/sagasmith/graph/bootstrap.py`** — extend `AgentServices` with a `skill_store: SkillStore | None = None` field (post-init default). Add a helper:
    ```python
    from pathlib import Path

    def _default_skill_store(*, first_slice_only: bool = False) -> "SkillStore":
        from sagasmith.skills_adapter import SkillStore
        from sagasmith.skills_adapter.errors import SkillValidationError
        # Resolve production roots relative to the sagasmith package
        import sagasmith
        pkg_root = Path(sagasmith.__file__).parent
        roots = [pkg_root / "agents", pkg_root / "skills"]
        store = SkillStore(roots=roots, first_slice_only=first_slice_only)
        store.scan()
        if store.errors:
            error_list = "\n".join(f"  - {p}: {msg}" for p, msg in store.errors)
            raise SkillValidationError(f"Production SKILL.md scan found errors:\n{error_list}")
        return store
    ```

    Update `AgentServices.__post_init__` (or `GraphBootstrap.from_services`) to call `_default_skill_store()` lazily if `skill_store is None`. When a test passes `skill_store=SkillStore(roots=[])`, the empty store bypasses validation.

    **Tests in `tests/skills_adapter/test_production_catalog.py`** (Tests 1-8):
    - Scan production roots; assert zero errors.
    - Required-set containment (Test 2) — NOT exact count.
    - For Test 5, use `monkeypatch` to temporarily replace one SKILL.md's content with invalid frontmatter, call `_default_skill_store()`, assert `SkillValidationError` raised.
    - Test 8: `uv run python -c "import sagasmith.graph.bootstrap; print('ok')"` exits 0 without triggering skill scan (lazy construction).
  </action>
  <verify>
    <automated>uv run pytest tests/skills_adapter/test_production_catalog.py tests/skills_adapter/ -q &amp;&amp; uv run pyright src/sagasmith/skills_adapter src/sagasmith/graph/bootstrap.py &amp;&amp; uv run ruff check src/sagasmith/skills_adapter src/sagasmith/graph &amp;&amp; uv run python -c "from sagasmith.skills_adapter import SkillStore; from pathlib import Path; s = SkillStore(roots=[Path('src/sagasmith/agents'), Path('src/sagasmith/skills')]); s.scan(); assert not s.errors, s.errors; names_by_scope = {scope: {r.name for r in recs} for scope, recs in s.skills.items()}; print(names_by_scope)"</automated>
  </verify>
  <done>
    14 production SKILL.md files ship with valid frontmatter; SkillStore scan returns zero errors; required-set containment test passes for all first-slice skills; `_default_skill_store()` fails loudly on production errors; bootstrap remains cheap at import time.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire set_skill calls into every agent node via contextvar handoff + load_skill for rules_lawyer</name>
  <files>
    src/sagasmith/graph/bootstrap.py,
    src/sagasmith/graph/__init__.py,
    src/sagasmith/agents/onboarding/node.py,
    src/sagasmith/agents/oracle/node.py,
    src/sagasmith/agents/rules_lawyer/node.py,
    src/sagasmith/agents/orator/node.py,
    src/sagasmith/agents/archivist/node.py,
    tests/agents/test_nodes_with_skills.py
  </files>
  <read_first>
    src/sagasmith/graph/activation_log.py,
    src/sagasmith/graph/bootstrap.py,
    src/sagasmith/graph/runtime.py,
    src/sagasmith/agents/onboarding/node.py,
    src/sagasmith/agents/oracle/node.py,
    src/sagasmith/agents/rules_lawyer/node.py,
    src/sagasmith/agents/orator/node.py,
    src/sagasmith/agents/archivist/node.py,
    src/sagasmith/skills_adapter/loader.py,
    src/sagasmith/skills_adapter/store.py,
    tests/graph/test_checkpoints.py
  </read_first>
  <behavior>
    - Test 1 (oracle set_skill): inside a persistent-graph run, after an oracle node invocation, `agent_skill_log.list_for_turn(turn_id)` has the oracle row with `skill_name="scene-brief-composition"`. No-op if `services.skill_store` does not have the skill (defensive — no crash).
    - Test 2 (rules_lawyer load_skill + set_skill on trigger match): when `pending_player_input == "roll perception"`, rules_lawyer_node calls `load_skill(services.skill_store, "skill-check-resolution", agent_name="rules_lawyer")` and `get_current_activation().set_skill("skill-check-resolution")`. The CheckResult is produced as before.
    - Test 3 (rules_lawyer no-match no-op): when input doesn't match, rules_lawyer_node does NOT call load_skill; set_skill is called with "degree-of-success" only if the node also invokes compute_degree (which in Phase 4 it doesn't for no-match path) — SO: no set_skill call at all for no-match; agent_skill_log row for that turn's rules_lawyer has `skill_name IS NULL`.
    - Test 4 (orator set_skill): orator_node calls `set_skill("scene-rendering")` on entry.
    - Test 5 (archivist set_skill): archivist_node calls `set_skill("turn-close-persistence")` when `services.skill_store` includes it; this is always true in production.
    - Test 6 (onboarding set_skill): onboarding_node calls `set_skill("onboarding-phase-wizard")` when returning `{"phase": "onboarding"}` (incomplete profile path).
    - Test 7 (graceful when no activation): when nodes are invoked OUTSIDE a `with AgentActivationLogger(...)` scope (e.g., unit tests that build a non-persistent graph), `get_current_activation()` returns None and `set_skill` is skipped gracefully (no AttributeError). Test: call oracle_node directly in a unit test, assert no exception.
    - Test 8 (end-to-end first_slice_only play turn): build `GraphRuntime` with `skill_store=_default_skill_store(first_slice_only=True)`. Run a full play turn (invoke_turn → resume_and_close) with `pending_player_input="roll perception"`. Assert:
      - turn_records.status == "complete"
      - agent_skill_log has 4 rows with `skill_name` set to: oracle → "scene-brief-composition", rules_lawyer → "skill-check-resolution", orator → "scene-rendering", archivist → "turn-close-persistence".
      - memory-packet-assembly (first_slice=false) is NOT in the store's catalog and was NOT loaded.
  </behavior>
  <action>
    **Update every agent node** to use the contextvar handoff. Pattern:

    ```python
    from sagasmith.graph.activation_log import get_current_activation

    def <agent>_node(state, services):
        activation = get_current_activation()

        # ... existing node logic ...

        if activation is not None:
            activation.set_skill("<skill-name>")

        return <result>
    ```

    **Node-specific skill bindings:**

    `oracle/node.py`:
    - Before returning the canned SceneBrief stub, call `activation.set_skill("scene-brief-composition")` if `activation is not None` and `services.skill_store.find(name="scene-brief-composition", agent_scope="oracle") is not None`.
    - If the skill is NOT available in the store (e.g., unit tests with empty store), skip the set_skill call silently. Do NOT fail the turn — stub fallback is acceptable.

    `rules_lawyer/node.py`:
    - When trigger phrase matches, BEFORE building the CheckResult:
      ```python
      from sagasmith.skills_adapter import load_skill
      from sagasmith.skills_adapter.errors import SkillNotFoundError, UnauthorizedSkillError
      try:
          load_skill(services.skill_store, "skill-check-resolution", agent_name="rules_lawyer")
          if activation is not None:
              activation.set_skill("skill-check-resolution")
      except (SkillNotFoundError, UnauthorizedSkillError):
          # Unit tests may run with an empty store; fall through to unskilled path
          pass
      ```
    - The CheckResult production logic is unchanged — deterministic handlers stay wired to DiceService + compute_degree regardless of skill loading. `load_skill` is audit plumbing in Phase 4.

    `orator/node.py`:
    - `activation.set_skill("scene-rendering")` on entry (when `activation is not None`).

    `archivist/node.py`:
    - `activation.set_skill("turn-close-persistence")` if the skill is available; else no-op.

    `onboarding/node.py`:
    - When returning `{"phase": "onboarding"}` (profile incomplete path), call `activation.set_skill("onboarding-phase-wizard")` (if activation + skill available).

    **Update `src/sagasmith/graph/bootstrap.py`** — ensure `AgentServices` exposes `skill_store` (added in Task 1). `GraphBootstrap.from_services` accepts `skill_store: SkillStore | None = None`; when None, defaults to `_default_skill_store()`. When called from a test that passes an empty store, respect that choice.

    **Update `src/sagasmith/graph/__init__.py`** to re-export `get_current_activation` (already done in Plan 04-02) and `_default_skill_store` for tests.

    **Tests in `tests/agents/test_nodes_with_skills.py`** (Tests 1-8):
    - Tests 1, 3-6 use a fixture that builds `GraphRuntime` with production skill_store (via `_default_skill_store()`), pre-inserts a turn_records row, invokes the runtime, and queries agent_skill_log.
    - Test 2 uses the same fixture but with `pending_player_input="roll perception"`.
    - Test 7 (standalone): build `AgentServices(skill_store=SkillStore(roots=[]))`, call `oracle_node(state, services)` directly outside any logger context; assert no exception, returns a valid dict.
    - Test 8 (E2E first_slice_only): `_default_skill_store(first_slice_only=True)`, build persistent graph, run one full turn, query log + checkpoint_refs + turn_records.

    Important: the node code MUST be defensive — `if activation is not None` and `services.skill_store.find(...) is not None` guards protect against test scenarios where the logger or store is absent.
  </action>
  <verify>
    <automated>uv run pytest tests/agents/ tests/graph/ tests/skills_adapter/ tests/integration/ tests/tui/ tests/persistence/ -q &amp;&amp; uv run pyright src/sagasmith/agents src/sagasmith/graph src/sagasmith/skills_adapter &amp;&amp; uv run ruff check src/sagasmith/agents src/sagasmith/graph src/sagasmith/skills_adapter &amp;&amp; uv run python -c "from sagasmith.graph import get_current_activation; from sagasmith.skills_adapter import SkillStore; from pathlib import Path; s = SkillStore(roots=[Path('src/sagasmith/agents'), Path('src/sagasmith/skills')]); s.scan(); assert not s.errors; print(f'skills={sum(len(v) for v in s.skills.values())}')"</automated>
  </verify>
  <done>
    Every agent node records a `skill_name` via the contextvar handoff when a logger + store are bound; rules_lawyer_node calls load_skill for skill-check-resolution on trigger match; nodes degrade gracefully when no activation/store is present (unit-test friendliness); end-to-end `first_slice_only=True` play turn completes with correct skill_names in agent_skill_log; full test suite green including 295 Phase 3 baseline + Plans 04-01 through 04-04.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production SKILL.md ↔ SkillStore.scan | Production scan errors silently disable skills unless bootstrap raises loud. |
| Node ↔ ContextVar `_current_activation` | If the contextvar handoff is misused (e.g., nested logger without reset), skill_names can cross-contaminate. |
| Node ↔ load_skill | The skill body is returned to node code; in Phase 4 nodes don't consume bodies, but Phase 6 will — body must be canary-scanned (done at scan time in Plan 04-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-33 | Tampering | Production SKILL.md scan has errors but silently degrades | mitigate | `_default_skill_store()` raises `SkillValidationError` if `store.errors` is non-empty. Developers see errors at startup. |
| T-04-34 | Information Disclosure | `skill_name` written to agent_skill_log leaks prompt content | mitigate | `set_skill` regex (from Plan 04-02) enforces kebab-case filesystem names only. Name comes from SKILL.md frontmatter, which is canary-scanned at scan time. |
| T-04-35 | Denial of Service | `first_slice_only=True` excludes a required skill, breaking a turn | mitigate | Required-set test in Plan 04-05 Task 1 asserts every first-slice required skill is present in `first_slice_only` mode. E2E test proves a full turn runs. |
| T-04-36 | Repudiation | Contextvar leaks skill_name across turns | mitigate | `AgentActivationLogger.__enter__/__exit__` sets and resets the contextvar token. Plan 04-02 Test 8 verifies isolation. |
| T-04-37 | Tampering | Adding a 15th skill breaks exact-count tests | mitigate | Tests use required-set containment, not exact counts (per Codex feedback). |

</threat_model>

<verification>

1. `uv run pytest -q` — full suite passes: Phase 3 (295) + Plans 04-01 through 04-05 additions.
2. `SkillStore` scan of production dirs returns 0 errors.
3. Required-set coverage test confirms all first-slice skills present.
4. End-to-end `first_slice_only=True` play turn completes with correct skill_names in agent_skill_log.
5. Node functions degrade gracefully outside activation-logger + skill-store context (unit-test compatibility).
6. Pyright + ruff clean across all modified modules.

</verification>

<success_criteria>

- SKILL-04: every node records its active skill via `get_current_activation().set_skill(...)`; `agent_skill_log.skill_name` populated after each turn.
- SKILL-05 end-to-end: a `first_slice_only=True` SkillStore + GraphRuntime still completes a full play turn.
- Required first-slice skills shipped: 3 cross-cutting + 4 Oracle + 3 RulesLawyer + 1 Orator + 1 Archivist (turn-close-persistence) + 1 Onboarding = 13 first_slice=true + 1 first_slice=false (memory-packet-assembly) = 14 total.
- Bootstrap surfaces SKILL.md scan errors loudly; developers see them at import/construction.
- Nodes retain defensive paths when no skill store or activation logger is present (unit tests don't break).
- No regression on Phase 3 baseline or prior Phase 4 plans.

</success_criteria>

<output>
After completion, create `.planning/phases/04-graph-runtime-and-agent-skills/04-05-SUMMARY.md` capturing:
- Shipped skill count by scope (use required-set containment language, NOT exact counts).
- Contextvar handoff pattern recap for Phase 6 (when LLM-driven nodes call `set_skill` around their LLM calls).
- Bootstrap validation behavior (raises on production scan errors).
- Deferred skills explicitly listed with target phases:
  - `encounter-budget-validation` (rules_lawyer) — Phase 5 (combat slice)
  - `retcon-aware-replay` (archivist) — Phase 8
  - `player-vault-sync` (archivist) — Phase 7
  - `player-fact-resolution` (archivist) — Phase 7
  - any additional skills from the specs not in the first-slice set
- Frontmatter subset constraints: multi-line descriptions not supported — if Phase 5+ needs them, revisit PyYAML tradeoff.
</output>
