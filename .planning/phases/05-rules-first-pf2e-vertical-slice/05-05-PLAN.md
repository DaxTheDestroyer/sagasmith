---
phase: 05-rules-first-pf2e-vertical-slice
plan: 05
type: execute
wave: 4
depends_on: [05-03, 05-04]
files_modified:
  - tests/integration/test_rules_first_vertical_slice.py
  - tests/evals/test_rules_first_qa_gate.py
  - src/sagasmith/evals/harness.py
autonomous: true
requirements: [RULE-04, RULE-05, RULE-06, RULE-07, RULE-08, RULE-09, RULE-10, RULE-11, RULE-12, TUI-07, QA-03]
must_haves:
  truths:
    - "Developer can run a no-paid-call vertical-slice regression covering /sheet, skill check, reveal roll details, and simple combat."
    - "QA-03 mechanics tests cover PF2e degree boundaries, natural 1/20 adjustment, seeded replay, skill checks, Strike, initiative, HP damage, and roll log completeness."
    - "The phase proves no LLM-authored math path is needed for first-slice mechanics."
    - "Roll logs and initiative order survive checkpoint/resume assertions for the first-slice combat path."
  artifacts:
    - path: "tests/integration/test_rules_first_vertical_slice.py"
      provides: "End-to-end no-paid-call Phase 5 vertical slice"
      contains: "test_rules_first_vertical_slice"
    - path: "tests/evals/test_rules_first_qa_gate.py"
      provides: "QA-03 coverage gate"
      contains: "test_qa03"
  key_links:
    - from: "tests/integration/test_rules_first_vertical_slice.py"
      to: "src/sagasmith/agents/rules_lawyer/node.py"
      via: "graph/TUI mechanics path"
      pattern: "rules_lawyer"
    - from: "tests/evals/test_rules_first_qa_gate.py"
      to: "src/sagasmith/services/combat_engine.py"
      via: "combat mechanics assertions"
      pattern: "resolve_strike"
---

<objective>
Add the Phase 5 verification spine that proves the full rules-first vertical slice works without paid calls.

Purpose: The phase is not complete until QA-03 and the roadmap success criteria are executable as tests, not just unit-level implementation pieces.
Output: Integration/eval tests and smoke harness registration for `/sheet`, skill check, reveal-mode audit output, simple combat, and no-LLM-math constraints.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-rules-first-pf2e-vertical-slice/05-RESEARCH.md
@docs/specs/PF2E_MVP_SUBSET.md
@src/sagasmith/evals/harness.py
@tests/integration/test_tui_graph_smoke.py
@tests/services/test_pf2e_degree.py
@tests/services/test_dice.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add end-to-end rules-first vertical-slice integration test</name>
  <files>tests/integration/test_rules_first_vertical_slice.py</files>
  <read_first>
    - tests/integration/test_tui_graph_smoke.py
    - src/sagasmith/tui/app.py
    - src/sagasmith/tui/commands/control.py
    - src/sagasmith/agents/rules_lawyer/node.py
    - src/sagasmith/tui/widgets/dice_overlay.py
  </read_first>
  <behavior>
    - Test opens `/sheet` and observes `Character Sheet` plus `Valeros`.
    - Test submits `roll athletics dc 15`, observes one `CheckResult`, and renders compact roll line with matching persisted `roll_id`.
    - Test submits `start combat`, advances turns until `pc_valeros_first_slice` is active if needed, then a Strike, and observes initiative order, action count decrement, attack roll id, damage roll id, and HP delta.
    - Test uses fake/no LLM services only.
  </behavior>
  <action>Create `tests/integration/test_rules_first_vertical_slice.py` with a single async Textual/graph smoke test named `test_rules_first_vertical_slice_sheet_check_reveal_and_combat`. Follow existing `tests/integration/test_tui_graph_smoke.py` app construction patterns (real in-memory SQLite, `apply_migrations`, seeded `turn_records`, real `GraphRuntime` via `build_persistent_graph`, deterministic `DiceService` and `CostGovernor`, no `OpenRouterClient`). Bind the graph runtime, run `/sheet`, a skill check input `roll athletics dc 15`, `start combat`, advance with `end turn` until `pc_valeros_first_slice` is active if initiative puts an enemy first, and then run `strike enemy_weak_melee with longsword`. Assert visible/rendered outputs include `Character Sheet`, `Saved to roll log:`, `roll_`, `Round: 1`, `Actions: 2/3`, `damage_roll=roll_`, and an HP delta containing `HP`. Add **two** checkpoint/resume assertions using the existing Phase 4 checkpointer (`graph_runtime.graph.get_state(thread_config)` after each step): (1) after `start combat`, resume and assert `initiative_order` and the skill/initiative `roll_id`s are unchanged; (2) after the Strike resolves, resume and assert both the attack `CheckResult.roll_result.roll_id` and the damage `RollResult.roll_id` are present in the persisted state and that the post-resume `combatants[*].current_hp` matches the pre-resume value (HP delta survives checkpoint). Addresses review concerns: test must not assume PC wins initiative and must verify persisted auditability of both attack and damage rolls. Do not invoke OpenRouter or any paid provider.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_rules_first_vertical_slice.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_rules_first_vertical_slice.py` contains `test_rules_first_vertical_slice_sheet_check_reveal_and_combat`
    - Test file contains `roll athletics dc 15`, `start combat`, `end turn`, and `strike enemy_weak_melee with longsword`
    - Test file asserts `damage_roll=roll_` and unchanged `initiative_order` after resume
    - Test file asserts post-Strike resume preserves both attack and damage `roll_id`s and the target's `current_hp`
    - Test file does not contain `OpenRouterClient`
  </acceptance_criteria>
  <done>The integrated player path proves `/sheet`, skill check, reveal audit cue, and simple combat work together without paid calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add QA-03 mechanics coverage gate and smoke harness registration</name>
  <files>tests/evals/test_rules_first_qa_gate.py, src/sagasmith/evals/harness.py</files>
  <read_first>
    - src/sagasmith/evals/harness.py
    - tests/services/test_pf2e_degree.py
    - tests/services/test_dice.py
    - tests/services/test_rules_engine.py
    - tests/services/test_combat_engine.py
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - QA gate test invokes real deterministic services/scenarios for PF2e degree boundaries, natural 1/20 adjustment, seeded replay, skill checks, Strike, initiative, HP damage, damage roll logging, and roll log completeness; it is not a hardcoded label-only test.
    - Smoke harness includes a Phase 5 no-paid-call rules-first check entry named `rules_first_vertical_slice`.
  </behavior>
  <action>Create `tests/evals/test_rules_first_qa_gate.py` with one test per QA-03 scenario rather than a single combined "label list" test. Each test calls the deterministic functions/services directly and asserts behavior: `test_qa03_degree_boundaries` (call `compute_degree` for natural 1/20 adjustment and exact-DC boundary cases); `test_qa03_seeded_replay` (call `DiceService.roll_d20` and `DiceService.roll(die="d8")` twice with identical seeds/inputs and assert identical `roll_id`/`natural`/`total`); `test_qa03_skill_check` (call `RulesEngine.resolve_check` and assert `CheckResult.roll_result.roll_id` matches the proposal-derived id); `test_qa03_initiative` (call `CombatEngine.start_encounter` and assert each combatant has an initiative `CheckResult.roll_result.roll_id` and the order is deterministic under tiebreakers); `test_qa03_strike_and_hp_damage` (call `CombatEngine.resolve_strike`, assert attack `CheckResult`, damage `RollResult.roll_id`, HP `StateDelta`, clamped HP, and effect text containing `damage_roll=`); `test_qa03_roll_log_completeness` (run a Strike sequence and assert every mechanical roll produced — initiative for each combatant, attack, damage on hit — has a unique persisted `roll_id` and no LLM-authored numeric value appears). Do **not** assert a hardcoded label set; coverage is proven by the existence and passing of these named tests. Update `src/sagasmith/evals/harness.py` to add a no-paid-call smoke check `rules_first_vertical_slice` that runs deterministic sheet/check/combat construction and returns a pass/fail result using existing harness result conventions. Preserve existing smoke checks and CLI behavior. Addresses review concern: QA-03 must test behavior, not labels.</action>
  <verify>
    <automated>uv run pytest tests/evals/test_rules_first_qa_gate.py tests/integration/test_rules_first_vertical_slice.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/evals/test_rules_first_qa_gate.py` contains the named tests `test_qa03_degree_boundaries`, `test_qa03_seeded_replay`, `test_qa03_skill_check`, `test_qa03_initiative`, `test_qa03_strike_and_hp_damage`, and `test_qa03_roll_log_completeness`, each calling real services rather than a hardcoded label set
    - `src/sagasmith/evals/harness.py` contains `rules_first_vertical_slice`
    - `uv run pytest tests/evals/test_rules_first_qa_gate.py -q` exits 0
  </acceptance_criteria>
  <done>QA-03 is represented by executable tests and the no-paid-call smoke harness includes the Phase 5 rules-first path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke/eval harness → provider layer | Verification must not perform paid LLM calls. |
| test fixtures → persisted/rendered mechanics | Tests create mechanics state that mirrors persisted audit expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure | eval/smoke output | mitigate | Use deterministic fake/no-provider services; assert no `OpenRouterClient` in Phase 5 integration test. |
| T-05-14 | Repudiation | QA-03 gate | mitigate | Cover each QA-03 area with a named, scenario-driven test that calls real deterministic services, so coverage cannot be faked by a label set. |
| T-05-15 | Tampering | smoke harness registration | mitigate | Preserve existing harness result conventions and add one named check rather than replacing existing checks. |
</threat_model>

<verification>
Run `uv run pytest tests/evals/test_rules_first_qa_gate.py tests/integration/test_rules_first_vertical_slice.py -q`, then `uv run ruff check src tests && uv run pyright src tests`.
</verification>

<success_criteria>
- Phase 5 has an end-to-end no-paid-call vertical-slice test.
- QA-03 coverage is explicit and executable.
- Smoke harness can exercise the rules-first mechanics path without provider calls.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rules-first-pf2e-vertical-slice/05-05-SUMMARY.md`
</output>
