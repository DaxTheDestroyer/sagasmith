---
phase: 05-rules-first-pf2e-vertical-slice
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/sagasmith/tui/widgets/sheet.py
  - src/sagasmith/tui/widgets/dice_overlay.py
  - src/sagasmith/tui/widgets/status_panel.py
  - src/sagasmith/tui/commands/control.py
  - tests/tui/test_sheet_command.py
  - tests/tui/test_dice_overlay.py
  - tests/tui/test_combat_status.py
autonomous: true
requirements: [RULE-04, RULE-06, RULE-07, RULE-08, RULE-09, RULE-10, RULE-11, TUI-07]
must_haves:
  truths:
    - "User can type /sheet and inspect the full read-only level-1 martial pregen."
    - "Reveal-mode dice details show DC, modifier, d20, total, degree, and persisted roll_id."
    - "Status panel shows HP, last rolls, and combat state details as plain terminal text."
    - "The /sheet view uses live character state first, so combat HP changes are visible instead of resetting to factory defaults."
  artifacts:
    - path: "src/sagasmith/tui/widgets/sheet.py"
      provides: "Character sheet renderer"
      contains: "render_character_sheet"
    - path: "src/sagasmith/tui/widgets/dice_overlay.py"
      provides: "Reveal-mode dice detail renderer"
      contains: "render_reveal_check"
    - path: "src/sagasmith/tui/widgets/status_panel.py"
      provides: "Combat/status text rendering"
      contains: "Active combatant"
  key_links:
    - from: "src/sagasmith/tui/commands/control.py"
      to: "current app/graph state"
      via: "SheetCommand reads live character_sheet before factory fallback"
      pattern: "character_sheet"
    - from: "src/sagasmith/tui/widgets/dice_overlay.py"
      to: "src/sagasmith/schemas/mechanics.py"
      via: "CheckResult input"
      pattern: "CheckResult"
---

<objective>
Replace Phase 3 TUI stubs with the approved Phase 5 mechanics surfaces: `/sheet`, reveal-mode dice detail text, and combat-aware status panel output.

Purpose: The player-facing TUI must expose deterministic mechanics audit data without relying on color-only cues, web components, or LLM prose.
Output: Plain-text renderers and command/status tests satisfying the approved UI-SPEC.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-rules-first-pf2e-vertical-slice/05-UI-SPEC.md
@.planning/phases/03-cli-setup-onboarding-and-tui-controls/03-04-SUMMARY.md
@src/sagasmith/tui/commands/control.py
@src/sagasmith/tui/widgets/status_panel.py
@src/sagasmith/tui/widgets/narration.py
@src/sagasmith/tui/state.py
@src/sagasmith/schemas/mechanics.py

<interfaces>
Existing `SheetCommand.handle(app, args)` writes to `NarrationArea` through `_write(app, line)`. Preserve command registry shape: dataclass with `name`, `description`, `handle(app, args)`.

Approved UI-SPEC exact strings required:
- Sheet title: `Character Sheet`
- Sheet footer: `Esc closes sheet. Type an action when ready.`
- Dice title: `Reveal Check`
- Dice formula label: `d20 + modifier vs DC`
- Dice compact transcript: `[roll] {reason}: d20 {natural} + {modifier} = {total} vs DC {dc} — {degree} ({roll_id})`
- Degree storage labels: `critical_success`, `success`, `failure`, `critical_failure`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the first-slice character sheet through /sheet</name>
  <files>src/sagasmith/tui/widgets/sheet.py, src/sagasmith/tui/commands/control.py, tests/tui/test_sheet_command.py</files>
  <read_first>
    - src/sagasmith/tui/commands/control.py
    - src/sagasmith/tui/widgets/narration.py
    - src/sagasmith/rules/first_slice.py
    - .planning/phases/05-rules-first-pf2e-vertical-slice/05-UI-SPEC.md
  </read_first>
  <behavior>
    - `/sheet` writes a stable plain-text sheet containing groups in order: Identity, Durability, Perception and saves, Skills, Attacks, Inventory, Footer.
    - The output contains exact title `Character Sheet` and footer `Esc closes sheet. Type an action when ready.`.
    - `/sheet` reads live `character_sheet` from app/graph state when present and falls back to `make_first_slice_character()` only when no current sheet exists; it does not consume combat actions and does not mutate `app.state` or graph state.
  </behavior>
  <action>Create `render_character_sheet(sheet: CharacterSheet) -> str` in `src/sagasmith/tui/widgets/sheet.py`. Format stable headings exactly: `Character Sheet`, `Identity`, `Durability`, `Perception and saves`, `Skills`, `Attacks`, `Inventory`, `Esc closes sheet. Type an action when ready.`. Update `SheetCommand.handle` to resolve the current sheet in this order: `app.graph_state["character_sheet"]` if such an attribute/key exists, then `app.current_state["character_sheet"]` if present, then `app.state.character_sheet` if present, and only then `make_first_slice_character()`. Validate dict sheets with `CharacterSheet.model_validate(...)` before rendering. Append each line or the full block to `NarrationArea`. Remove the Phase 5 stub text from `/sheet`. Addresses review concern: `/sheet` must render live HP/current state, not a fresh factory sheet after combat. Do not expose hidden DCs or enemy data.</action>
  <verify>
    <automated>uv run pytest tests/tui/test_sheet_command.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/tui/widgets/sheet.py` contains `def render_character_sheet(sheet: CharacterSheet) -> str:`
    - `src/sagasmith/tui/commands/control.py` no longer contains `/sheet (stub`
    - `tests/tui/test_sheet_command.py` asserts a live sheet with `current_hp=13` renders `13/20`, not factory `20/20`
    - `tests/tui/test_sheet_command.py` asserts `Character Sheet`, `Identity`, `Durability`, `Perception and saves`, `Skills`, `Attacks`, and `Inventory`
  </acceptance_criteria>
  <done>User can inspect the pregen PC via `/sheet` as a read-only mechanics view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reveal-mode dice detail renderer</name>
  <files>src/sagasmith/tui/widgets/dice_overlay.py, tests/tui/test_dice_overlay.py</files>
  <read_first>
    - src/sagasmith/schemas/mechanics.py
    - .planning/phases/05-rules-first-pf2e-vertical-slice/05-UI-SPEC.md
  </read_first>
  <behavior>
    - `render_reveal_check(result, reason="force stuck gate")` includes `Reveal Check`, `d20 + modifier vs DC`, DC, modifier, natural d20, total, degree, roll_id, and `Saved to roll log: {roll_id}`.
    - `render_compact_roll_line(result, reason)` returns exact pattern `[roll] {reason}: d20 {natural} + {modifier} = {total} vs DC {dc} — {degree} ({roll_id})`.
    - Renderer does not roll dice; it only reads `CheckResult` and `RollResult` values; because Phase 5 checks are pre-resolved, copy uses `Press Enter to reveal` rather than implying a second roll.
  </behavior>
  <action>Create `src/sagasmith/tui/widgets/dice_overlay.py` with pure functions `render_reveal_check(result: CheckResult, *, reason: str) -> str` and `render_compact_roll_line(result: CheckResult, *, reason: str) -> str`. Implement Phase 5 as transcript-rendered reveal details, not a Textual `ModalScreen`; the equivalent modal requirement is satisfied by a stable reveal block followed by the compact persisted transcript line. Include strings `Reveal Check`, `d20 + modifier vs DC`, `Press Enter to reveal. /pause and /line remain available.`, `Saved to roll log: {roll_id}`, and `Enter: continue  Esc: close details  /pause: pause  /line: set a line`. Add a comment noting UI-SPEC allowed pre-resolved checks to reveal an already-recorded `RollResult`; no second roll occurs on display. Addresses review concern: reveal-mode behavior/copy was ambiguous. Do not import `DiceService` or call rolling functions in this file.</action>
  <verify>
    <automated>uv run pytest tests/tui/test_dice_overlay.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/tui/widgets/dice_overlay.py` contains `def render_reveal_check(` and `def render_compact_roll_line(`
    - `src/sagasmith/tui/widgets/dice_overlay.py` does not contain `roll_d20` or `DiceService`
    - `tests/tui/test_dice_overlay.py` asserts `Saved to roll log:` and the exact compact `[roll]` line
    - `tests/tui/test_dice_overlay.py` asserts `Press Enter to reveal` is present and `Press Enter to roll` is absent
  </acceptance_criteria>
  <done>Reveal-mode mechanics details are renderable from structured results without generating additional rolls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend status panel for combat state and last-roll audit summaries</name>
  <files>src/sagasmith/tui/widgets/status_panel.py, tests/tui/test_combat_status.py</files>
  <read_first>
    - src/sagasmith/tui/widgets/status_panel.py
    - src/sagasmith/tui/state.py
    - src/sagasmith/schemas/mechanics.py
    - .planning/phases/05-rules-first-pf2e-vertical-slice/05-UI-SPEC.md
  </read_first>
  <behavior>
    - Status output includes `Active combatant:`, `Round:`, `Actions: 3/3` style values, `Reaction: available` or `Reaction: spent`, and zero/one/two enemy cases without assuming exactly two enemies.
    - Last rolls remain limited to three summaries and include degree plus roll id suffix.
    - Output uses text labels; essential mechanics information is not color-only.
  </behavior>
  <action>Extend status rendering without changing the existing shell layout. Add helper `format_combat_status(combat_state: CombatState | None) -> list[str]` in `status_panel.py` and include its lines after `Last rolls`; callers that have dicts must validate with `CombatState.model_validate(...)` before calling, rather than weakening this renderer to `dict[str, object]`. Use exact labels: `Combat: —` when absent, otherwise `Round: {round_number}`, `Active combatant: {id}`, `Actions: {remaining}/3 for {active_combatant_id}`, `Reaction: available|spent`, `Positions: {id}={tag}, ...`, `Enemies: none` for zero enemies, `Enemies: {enemy1} HP {current}/{max}` for one enemy, and `Enemies: {enemy1} HP {current}/{max}; {enemy2} HP {current}/{max}` for two enemies. Preserve `StatusSnapshot.last_rolls[:3]` behavior. Addresses review concern: fixed two-enemy template and loose dict typing.</action>
  <verify>
    <automated>uv run pytest tests/tui/test_combat_status.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/tui/widgets/status_panel.py` contains `def format_combat_status(`
    - `tests/tui/test_combat_status.py` asserts `Actions: 3/3`, `Reaction: available`, and `Positions:`
    - `tests/tui/test_combat_status.py` asserts only three `Last rolls` entries render
    - `tests/tui/test_combat_status.py` asserts `Enemies: none`, one-enemy, and two-enemy renderings
  </acceptance_criteria>
  <done>Status panel carries persistent mechanics context for checks and combat while preserving Phase 3 layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| structured mechanics → TUI text | Mechanics values are rendered into player-visible transcript/status surfaces. |
| player command `/sheet` → sheet renderer | Command must not mutate combat action counts or reveal GM-only data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure | `/sheet` renderer | mitigate | Render only `CharacterSheet` PC data from `make_first_slice_character`; never read enemy or scene GM-only fields. |
| T-05-11 | Repudiation | dice overlay compact line | mitigate | Include persisted `roll_id` and test exact compact audit-line output. |
| T-05-12 | Spoofing | mechanics transcript text | mitigate | Build roll/sheet summaries from Pydantic models and keep untrusted narration markup disabled in existing `NarrationArea`. |
</threat_model>

<verification>
Run `uv run pytest tests/tui/test_sheet_command.py tests/tui/test_dice_overlay.py tests/tui/test_combat_status.py -q` and `uv run pyright src/sagasmith/tui tests/tui`.
</verification>

<success_criteria>
- `/sheet` renders all required groups in stable plain text.
- Reveal-mode roll details contain DC, modifier, d20, total, degree, and roll_id.
- Status panel displays combat and last-roll audit context without relying on color-only cues.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rules-first-pf2e-vertical-slice/05-04-SUMMARY.md`
</output>
