---
phase: 05-rules-first-pf2e-vertical-slice
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/sagasmith/services/dice.py
  - src/sagasmith/services/combat_engine.py
  - tests/services/test_dice.py
  - tests/services/test_combat_engine.py
autonomous: true
requirements: [RULE-06, RULE-07, RULE-08, RULE-09, RULE-10, RULE-11, RULE-12, QA-03]
must_haves:
  truths:
    - "User can complete a simple theater-of-mind combat with initiative, actions, positions, Strikes, and HP deltas."
    - "Combat uses no more than two enemies and only the position tags close, near, far, and behind_cover."
    - "Every initiative and Strike roll is auditable through structured CheckResult/RollResult data."
    - "Strike damage is rolled deterministically and logged with a roll_id; fixed damage literals are not used."
  artifacts:
    - path: "src/sagasmith/services/combat_engine.py"
      provides: "Deterministic first-slice combat state transitions"
      contains: "class CombatEngine"
  key_links:
    - from: "src/sagasmith/services/combat_engine.py"
      to: "src/sagasmith/services/rules_engine.py"
      via: "RulesEngine.resolve_check"
      pattern: "resolve_check"
    - from: "src/sagasmith/services/combat_engine.py"
      to: "src/sagasmith/schemas/mechanics.py"
      via: "CombatState and CheckResult"
      pattern: "CombatState"
---

<objective>
Implement deterministic first-slice combat mechanics on top of the rules foundation from Plan 05-01.

Purpose: Phase 5 success requires complete PF2e-inspired vertical-slice combat before AI narration, including initiative persistence, action economy, positions, Strikes, HP deltas, and roll auditability.
Output: `CombatEngine` with tests for initiative, action counts, valid position tags, Strike hit/miss/critical hit, HP changes, and encounter completion.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-rules-first-pf2e-vertical-slice/05-RESEARCH.md
@docs/specs/PF2E_MVP_SUBSET.md
@docs/specs/STATE_SCHEMA.md
@src/sagasmith/services/rules_engine.py
@src/sagasmith/rules/first_slice.py
@src/sagasmith/schemas/mechanics.py
@src/sagasmith/schemas/common.py

<interfaces>
From Plan 05-01 output:
```python
RulesEngine.resolve_check(sheet: CharacterSheet, *, stat: str, dc: int, reason: str, roll_index: int, kind: Literal["skill", "initiative"] = "skill", target_id: str | None = None) -> CheckResult
make_first_slice_character() -> CharacterSheet
make_first_slice_enemies() -> tuple[CombatantState, CombatantState]
```

From `src/sagasmith/schemas/mechanics.py`:
```python
class CombatState(SchemaModel):
    encounter_id: str
    round_number: int
    active_combatant_id: str
    initiative_order: list[InitiativeEntry]
    combatants: list[CombatantState]
    positions: dict[str, PositionTagValue]
    action_counts: dict[str, int]
    reaction_available: dict[str, bool]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add initiative and combat state setup</name>
  <files>src/sagasmith/services/dice.py, src/sagasmith/services/combat_engine.py, tests/services/test_dice.py, tests/services/test_combat_engine.py</files>
  <read_first>
    - src/sagasmith/services/rules_engine.py
    - src/sagasmith/rules/first_slice.py
    - src/sagasmith/schemas/mechanics.py
    - src/sagasmith/schemas/common.py
  </read_first>
  <behavior>
    - `CombatEngine.start_encounter(sheet, enemies, roll_index=0)` rejects more than two enemies.
    - It rolls Perception initiative for PC and enemies through deterministic dice inputs and returns `CombatState` with initiative_order sorted by total descending, then perception modifier descending, then actor_id ascending for deterministic ties.
    - It sets `round_number=1`, `active_combatant_id` to the top initiative entry, `action_counts` to `3` for each combatant, `reaction_available` to `True`, and positions using only `close`, `near`, `far`, `behind_cover`.
  </behavior>
  <action>Create frozen dataclass `CombatEngine` with fields `dice: DiceService` and `rules: RulesEngine`. Implement `start_encounter(sheet: CharacterSheet, enemies: tuple[CombatantState, ...], *, encounter_id: str = "enc_first_slice", roll_index: int = 0) -> tuple[CombatState, list[CheckResult]]`. Reject non-`CombatantState` enemy inputs with `ValueError("enemy records must be CombatantState")`. Set starting positions: PC `near`, weak melee `close`, weak ranged `far`. Produce initiative `CheckResult` records for PC and each enemy using Perception modifiers; enemy initiative may build `RollResult` directly with `DiceService.roll_d20(purpose="initiative", actor_id=enemy_id, modifier=enemy_perception_modifier, roll_index=roll_index + n, dc=None)` and degree `success` because initiative is ordering, not success/failure. Sort initiative by total descending, then perception modifier descending, then actor_id ascending. Addresses review concern: initiative tie-breaking and enemy data validation were unspecified. Do not add tactical grid, coordinates, or additional enemies.</action>
  <verify>
    <automated>uv run pytest tests/services/test_combat_engine.py::test_start_encounter_rolls_initiative_and_sets_action_state -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/services/combat_engine.py` contains `def start_encounter(`
    - `src/sagasmith/services/combat_engine.py` contains `if len(enemies) > 2:`
    - `tests/services/test_combat_engine.py` asserts `action_counts` values are all `3`
    - `tests/services/test_combat_engine.py` asserts positions are subset of `{ "close", "near", "far", "behind_cover" }`
    - `tests/services/test_combat_engine.py` asserts initiative ties break by perception modifier then actor_id
  </acceptance_criteria>
  <done>Combat starts with auditable initiative order, persisted-state-compatible `CombatState`, three actions, reaction availability, and valid theater-of-mind positions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Strike, movement, action consumption, and encounter completion</name>
  <files>src/sagasmith/services/combat_engine.py, tests/services/test_combat_engine.py</files>
  <read_first>
    - src/sagasmith/services/combat_engine.py
    - src/sagasmith/services/pf2e.py
    - src/sagasmith/schemas/deltas.py
    - docs/specs/PF2E_MVP_SUBSET.md
  </read_first>
  <behavior>
    - `resolve_strike(state, actor_id, target_id, attack_id, roll_index)` consumes one action, rolls d20 against target AC, rolls damage through `DiceService.roll(die="d8"|"d6", ...)` on hit/critical hit, clamps HP at 0, and returns updated `CombatState` plus attack `CheckResult` and damage `RollResult | None` so the audit log can persist both mechanical rolls.
    - Strike targeting matrix: melee can target `close` only; melee against `near`, `far`, or `behind_cover` raises `ValueError("{target} is {position}; this action requires close range")`; ranged can target `close`, `near`, or `far`; ranged against `behind_cover` applies `+2` circumstance AC and records `behind_cover` in the effect description.
    - `move(state, actor_id, new_position)` consumes one action and only accepts `close`, `near`, `far`, `behind_cover`. Moving to the actor's current position raises `ValueError("{actor_id} is already at {position}")` and consumes no action.
    - `end_turn(state)` advances active combatant, skips defeated combatants at 0 HP, resets the new active combatant to 3 actions and `reaction_available=True`, increments round when wrapping to the first living combatant, and keeps initiative order stable.
    - `is_encounter_complete(state)` returns true when all enemies or the PC are at 0 HP.
  </behavior>
  <action>`DiceService.roll(die="d8"|"d6", ...)` already exists in `src/sagasmith/services/dice.py` and produces a `RollResult` with stable `roll_id`; do not add a new method. Add `tests/services/test_dice.py` cases for `d8` and `d6` seeded replay (same inputs → identical `RollResult`) if not already covered. Extend `CombatEngine` with `resolve_strike`, `move`, `end_turn`, and `is_encounter_complete`. Damage expression parsing for Phase 5 must support exactly `1d8+4 slashing` and `1d6 piercing`: roll `1d8` with modifier `4` for longsword hits and `1d6` with modifier `0` for shortbow hits via `DiceService.roll`. On `critical_success` perform exactly **one** damage roll and double the resulting `RollResult.total` (PF2e RAW: "double the damage" doubles the total including modifier). Record the doubled value in the `Effect.description`/`StateDelta.value` but persist the single underlying `RollResult` once so the audit log has exactly one damage `roll_id` per Strike. Use `0` on miss/critical_failure with no damage `RollResult`. Return signature should expose the damage roll without hiding it, e.g. `tuple[CombatState, CheckResult, RollResult | None]`; update downstream plans/tests to append the damage `roll_id` to roll-log audit output. Multiple Attack Penalty, reactions, and advanced actions remain out of Phase 5 per REQUIREMENTS; do not implement them, but leave `reaction_available` reset semantics correct for future use. Create `Effect(kind="hp_delta", description="{target}: HP {old_hp} -> {new_hp} ({delta_label}; damage_roll={damage_roll_id})", target_id=target_id)` and `StateDelta(source="rules", path="combatants.{target_id}.current_hp", operation="set", value=new_hp, reason="Strike damage")` for damaging hits. Raise `ValueError("No actions remaining for {combatant}")` at zero actions and `ValueError("{target} is {position}; this action requires close range")` for invalid melee range. Addresses review concerns: damage auditability, reaction reset, defeated-combatant handling, ranged-position matrix, and combat edge cases.</action>
  <verify>
    <automated>uv run pytest tests/services/test_dice.py tests/services/test_combat_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/services/combat_engine.py` contains `def resolve_strike(`, `def move(`, `def end_turn(`, and `def is_encounter_complete(`
    - `tests/services/test_combat_engine.py` asserts hit, miss, and critical hit outcomes
    - `tests/services/test_combat_engine.py` asserts HP never drops below 0
    - `tests/services/test_combat_engine.py` asserts invalid melee range error text contains `requires close range`
    - `tests/services/test_combat_engine.py` asserts hit and critical hit effects include `damage_roll=roll_`
    - `tests/services/test_combat_engine.py` asserts a critical hit damage delta equals `2 * single_damage_roll_total` and that exactly one damage `RollResult` is produced (one `roll_id`)
    - `tests/services/test_combat_engine.py` asserts `move` to the actor's current position raises `ValueError` containing `already at` and does not decrement `action_counts`
    - `tests/services/test_combat_engine.py` asserts `end_turn` skips 0 HP combatants and resets `reaction_available` to `True` for the new active combatant
  </acceptance_criteria>
  <done>First-slice combat can progress through initiative, actions, movement, Strikes, HP deltas, turn advancement, and completion using deterministic structured data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player action intent → CombatEngine | Untrusted target/action choices could bypass range, action count, or enemy limit. |
| CombatEngine → state deltas | HP changes and action counts become persisted/replayable mechanics state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | `resolve_strike` | mitigate | Validate actor, target, attack_id, remaining actions, and position constraints before roll or HP mutation. |
| T-05-05 | Repudiation | combat roll log | mitigate | Return `CheckResult` for initiative and every Strike; tests assert roll ids are present. |
| T-05-06 | Denial of Service | encounter setup | mitigate | Reject `len(enemies) > 2` and unsupported position tags with `ValueError`. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_dice.py tests/services/test_combat_engine.py -q` plus `uv run pyright src/sagasmith/services/dice.py src/sagasmith/services/combat_engine.py tests/services/test_dice.py tests/services/test_combat_engine.py`.
</verification>

<success_criteria>
- Initiative order persists in `CombatState`.
- Action counts and reaction availability are tracked per combatant.
- Strikes apply hit/miss/critical hit damage and HP deltas with structured attack and damage roll audit records.
- Theater-of-mind combat supports no more than two enemies and only the four approved position tags.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rules-first-pf2e-vertical-slice/05-02-SUMMARY.md`
</output>
