---
phase: 05-rules-first-pf2e-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sagasmith/rules/first_slice.py
  - src/sagasmith/rules/__init__.py
  - src/sagasmith/services/rules_engine.py
  - tests/rules/test_first_slice_data.py
  - tests/services/test_rules_engine.py
autonomous: true
requirements: [RULE-04, RULE-05, RULE-11, RULE-12, QA-03]
must_haves:
  truths:
    - "User can inspect a valid level-1 pregenerated martial character sheet."
    - "A skill or Perception check resolves from deterministic sheet data, fixed DC, DiceService, and compute_degree."
    - "Every skill/check result carries an auditable roll_id and no LLM-authored math."
  artifacts:
    - path: "src/sagasmith/rules/first_slice.py"
      provides: "One level-1 martial CharacterSheet and two first-slice enemy records"
      contains: "make_first_slice_character"
    - path: "src/sagasmith/services/rules_engine.py"
      provides: "Deterministic CheckProposal/CheckResult resolution"
      contains: "resolve_check"
  key_links:
    - from: "src/sagasmith/services/rules_engine.py"
      to: "src/sagasmith/services/dice.py"
      via: "DiceService.roll_d20"
      pattern: "roll_d20"
    - from: "src/sagasmith/services/rules_engine.py"
      to: "src/sagasmith/services/pf2e.py"
      via: "compute_degree"
      pattern: "compute_degree"
---

<objective>
Build the deterministic first-slice rules foundation: legal pregen data, local enemy data, skill/Perception check resolution, and auditable roll-log-shaped outputs.

Purpose: Phase 5 needs real mechanics before graph or TUI wiring so no downstream surface invents modifiers, DCs, degrees, damage, or roll identifiers.
Output: `sagasmith.rules` data helpers and `RulesEngine.resolve_check()` with tests proving RULE-04, RULE-05, RULE-11, RULE-12, and QA-03 coverage.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rules-first-pf2e-vertical-slice/05-RESEARCH.md
@.planning/phases/05-rules-first-pf2e-vertical-slice/05-UI-SPEC.md
@docs/specs/PF2E_MVP_SUBSET.md
@docs/specs/STATE_SCHEMA.md
@src/sagasmith/schemas/mechanics.py
@src/sagasmith/schemas/common.py
@src/sagasmith/services/dice.py
@src/sagasmith/services/pf2e.py

<interfaces>
From `src/sagasmith/schemas/mechanics.py`:
```python
class CharacterSheet(SchemaModel):
    id: str; name: str; level: int; ancestry: str; background: str; class_name: str
    abilities: dict[str, int]; proficiencies: dict[str, Literal[...]]
    max_hp: int; current_hp: int; armor_class: int; perception_modifier: int
    saving_throws: dict[str, int]; skills: dict[str, int]; attacks: list[AttackProfile]
    inventory: list[InventoryItem]; conditions: list[ConditionInstance]

class CheckProposal(SchemaModel):
    id: str; reason: str; kind: Literal["skill", "attack", "save", "initiative", "flat"]
    actor_id: str; target_id: str | None; stat: str; modifier: int; dc: int | None; secret: bool

class CheckResult(SchemaModel):
    proposal_id: str; roll_result: RollResult; degree: Literal["critical_success", "success", "failure", "critical_failure"]
    effects: list[Effect]; state_deltas: list[StateDelta]
```

From `src/sagasmith/services/dice.py`:
```python
DiceService.roll_d20(*, purpose: str, actor_id: str, modifier: int, roll_index: int, dc: int | None = None) -> RollResult
```

From `src/sagasmith/services/pf2e.py`:
```python
def compute_degree(natural: int, total: int, dc: int) -> Degree
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add first-slice character and enemy data helpers</name>
  <files>src/sagasmith/rules/first_slice.py, src/sagasmith/rules/__init__.py, tests/rules/test_first_slice_data.py</files>
  <read_first>
    - src/sagasmith/schemas/mechanics.py
    - src/sagasmith/schemas/common.py
    - docs/specs/PF2E_MVP_SUBSET.md
    - docs/specs/STATE_SCHEMA.md
  </read_first>
  <behavior>
    - `make_first_slice_character()` returns a valid `CharacterSheet` with id `pc_valeros_first_slice`, name `Valeros`, level `1`, class_name `Fighter`, current_hp `20`, max_hp `20`, armor_class `18`, perception_modifier `5`, four trained skills first in insertion order: `athletics`, `intimidation`, `survival`, `acrobatics`.
    - The sheet has exactly two attacks: melee `longsword` with modifier `7`, damage `1d8+4 slashing`; ranged `shortbow` with modifier `5`, damage `1d6 piercing`, range `60 feet`.
    - `make_first_slice_enemies()` returns exactly two `CombatantState`-compatible records with ids `enemy_weak_melee` and `enemy_weak_ranged`, each with `armor_class`, `max_hp`, `current_hp`, `perception_modifier`, `attacks`, `saves`, and `xp_value` source data.
  </behavior>
  <action>Create `src/sagasmith/rules/first_slice.py` with pure factory functions `make_first_slice_character() -> CharacterSheet` and `make_first_slice_enemies() -> tuple[dict[str, object], dict[str, object]]`. Use only ORC-compatible generic labels: Valeros-style level-1 martial PC data, weak melee enemy, weak ranged enemy. Export both functions from `src/sagasmith/rules/__init__.py`. Do not add character creation UI or unsupported equipment actions.</action>
  <verify>
    <automated>uv run pytest tests/rules/test_first_slice_data.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/rules/first_slice.py` contains `def make_first_slice_character() -> CharacterSheet:`
    - `src/sagasmith/rules/first_slice.py` contains `pc_valeros_first_slice`
    - `src/sagasmith/rules/first_slice.py` contains both `enemy_weak_melee` and `enemy_weak_ranged`
    - `tests/rules/test_first_slice_data.py` asserts exactly four trained skills and exactly two attacks
  </acceptance_criteria>
  <done>The pregen sheet and two enemy records validate through existing Pydantic mechanics models and expose stable IDs for later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic skill and Perception check resolution</name>
  <files>src/sagasmith/services/rules_engine.py, tests/services/test_rules_engine.py</files>
  <read_first>
    - src/sagasmith/services/dice.py
    - src/sagasmith/services/pf2e.py
    - src/sagasmith/schemas/mechanics.py
    - src/sagasmith/rules/first_slice.py
  </read_first>
  <behavior>
    - `RulesEngine.resolve_check(sheet, stat="athletics", dc=15, reason="force stuck gate", roll_index=0)` uses `sheet.skills["athletics"]` as modifier, calls `DiceService.roll_d20(..., dc=15)`, computes degree with `compute_degree`, and returns `CheckResult` whose `roll_result.dc == 15`.
    - `RulesEngine.resolve_check(sheet, stat="perception", dc=14, reason="notice ambush", roll_index=1)` uses `sheet.perception_modifier`.
    - Unsupported stats raise `ValueError` before rolling and produce no `CheckResult`.
  </behavior>
  <action>Create frozen dataclass `RulesEngine` in `src/sagasmith/services/rules_engine.py` with field `dice: DiceService`. Implement `build_check_proposal(sheet: CharacterSheet, *, stat: str, dc: int, reason: str, kind: Literal["skill", "initiative"] = "skill", target_id: str | None = None) -> CheckProposal` and `resolve_check(sheet: CharacterSheet, *, stat: str, dc: int, reason: str, roll_index: int, kind: Literal["skill", "initiative"] = "skill", target_id: str | None = None) -> CheckResult`. Modifier lookup rules: `perception` uses `sheet.perception_modifier`; any other stat must exist in `sheet.skills`. Set `secret=False`. Set `proposal_id` to `check_{stat}_{roll_index:06d}`. Use `DiceService.roll_d20(purpose=stat, actor_id=sheet.id, modifier=modifier, roll_index=roll_index, dc=dc)`. Effects and state_deltas are empty for skill/Perception checks. Do not use LLM text, scene prose, or random module APIs.</action>
  <verify>
    <automated>uv run pytest tests/services/test_rules_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/services/rules_engine.py` contains `class RulesEngine`
    - `src/sagasmith/services/rules_engine.py` contains `def resolve_check(`
    - `src/sagasmith/services/rules_engine.py` contains `compute_degree(` and `roll_d20(`
    - `tests/services/test_rules_engine.py` asserts unsupported stats raise `ValueError`
  </acceptance_criteria>
  <done>Skill and Perception checks resolve through deterministic sheet modifiers, DiceService, and compute_degree with stable audit output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player input → RulesEngine stat selection | Untrusted requested stat could attempt unsupported mechanics or hidden fields. |
| mechanics data → roll log/checkpoint | Structured roll results become persisted audit records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `RulesEngine.resolve_check` | mitigate | Reject stats not equal to `perception` and not present in `CharacterSheet.skills` before calling `DiceService`. |
| T-05-02 | Repudiation | `CheckResult.roll_result.roll_id` | mitigate | Use deterministic `DiceService.roll_d20` roll ids and require tests to assert `roll_id` stability. |
| T-05-03 | Information Disclosure | first-slice enemy helpers | accept | Enemy data is local generic mechanics data and not GM-only campaign canon; no secrets or provider metadata are included. |
</threat_model>

<verification>
Run `uv run pytest tests/rules/test_first_slice_data.py tests/services/test_rules_engine.py -q` and `uv run pyright src/sagasmith/rules src/sagasmith/services/rules_engine.py tests/rules tests/services/test_rules_engine.py`.
</verification>

<success_criteria>
- Pregen `CharacterSheet` validates and contains all `/sheet` groups required by the UI-SPEC.
- `RulesEngine.resolve_check` returns `CheckResult` for skill and Perception checks with persisted-audit-ready `RollResult`.
- Unsupported stats fail closed without rolling.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rules-first-pf2e-vertical-slice/05-01-SUMMARY.md`
</output>
