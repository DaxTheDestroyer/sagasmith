---
phase: 05-rules-first-pf2e-vertical-slice
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/sagasmith/agents/rules_lawyer/node.py
  - src/sagasmith/graph/routing.py
  - src/sagasmith/graph/state.py
  - src/sagasmith/tui/app.py
  - tests/agents/test_rules_lawyer_phase5.py
  - tests/graph/test_routing.py
autonomous: true
requirements: [RULE-05, RULE-06, RULE-07, RULE-08, RULE-09, RULE-10, RULE-11, RULE-12]
must_haves:
  truths:
    - "Natural-language or slash-style first-slice actions reach deterministic RulesLawyer resolution without LLM math."
    - "Combat phase routes into the graph instead of ending immediately."
    - "Graph-built play state carries the first-slice character sheet and mechanics results across turns."
    - "Unsupported or invalid first-slice inputs produce deterministic user-visible error state instead of silent `{}` or crashes."
  artifacts:
    - path: "src/sagasmith/agents/rules_lawyer/node.py"
      provides: "Rules-first intent handling for checks and combat"
      contains: "RulesEngine"
    - path: "src/sagasmith/graph/routing.py"
      provides: "Combat phase graph routing"
      contains: "Phase.COMBAT.value"
  key_links:
    - from: "src/sagasmith/agents/rules_lawyer/node.py"
      to: "src/sagasmith/services/rules_engine.py"
      via: "RulesEngine.resolve_check"
      pattern: "resolve_check"
    - from: "src/sagasmith/agents/rules_lawyer/node.py"
      to: "src/sagasmith/services/combat_engine.py"
      via: "CombatEngine"
      pattern: "CombatEngine"
---

<objective>
Wire Phase 5 deterministic mechanics into the LangGraph RulesLawyer node and combat routing.

Purpose: The graph must exercise real first-slice rules mechanics before AI story loop work, and the TUI must build play state with the required pregen sheet instead of `character_sheet=None`.
Output: RulesLawyer node supports first-slice checks and combat actions; combat graph route no longer terminates at `END`; tests prove no LLM-authored math path is used.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-graph-runtime-and-agent-skills/04-01-SUMMARY.md
@.planning/phases/04-graph-runtime-and-agent-skills/04-03-SUMMARY.md
@.planning/phases/05-rules-first-pf2e-vertical-slice/05-RESEARCH.md
@src/sagasmith/agents/rules_lawyer/node.py
@src/sagasmith/graph/routing.py
@src/sagasmith/tui/app.py
@src/sagasmith/graph/bootstrap.py

<interfaces>
Existing `AgentServices` from `src/sagasmith/graph/bootstrap.py`:
```python
@dataclass(frozen=True)
class AgentServices:
    dice: DiceService
    cost: CostGovernor
    safety: object | None = None
    llm: object | None = None
    skill_store: SkillStore | None = None
    _call_recorder: list[str] | None = None
```

Existing `SagaSmithApp._build_play_state(player_input)` returns a dict matching `SagaState`; Phase 5 must set `character_sheet` to `make_first_slice_character().model_dump()` and preserve existing cost/session fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace RulesLawyer trigger phrases with first-slice deterministic intent handling</name>
  <files>src/sagasmith/agents/rules_lawyer/node.py, tests/agents/test_rules_lawyer_phase5.py</files>
  <read_first>
    - src/sagasmith/agents/rules_lawyer/node.py
    - src/sagasmith/services/rules_engine.py
    - src/sagasmith/services/combat_engine.py
    - src/sagasmith/rules/first_slice.py
    - tests/agents/test_node_contracts.py
  </read_first>
  <behavior>
    - Inputs containing `athletics`, `acrobatics`, `survival`, `intimidation`, or `perception` plus `dc <number>` append exactly one deterministic `CheckResult`.
    - Input `start combat` creates `combat_state` with no more than two enemies and initiative CheckResults.
    - Input `strike <target_id> with <attack_id>` resolves through `CombatEngine.resolve_strike` when `combat_state` exists.
    - Unsupported input, parser mismatch, and rules-engine `ValueError` return deterministic error state in `pending_narration` and do not roll.
  </behavior>
  <action>Update `rules_lawyer_node` so it constructs `RulesEngine(dice=services.dice)` and `CombatEngine(dice=services.dice, rules=rules_engine)`. Preserve activation logging with `skill-check-resolution`; add `combat-resolution` skill activation only if a production skill exists and is authorized, using the same try/except pattern already present. Implement deterministic parsing with case-insensitive, whitespace-normalized, anchored regexes only: `^roll\s+(athletics|acrobatics|survival|intimidation|perception)\s+dc\s+(\d+)$`, `^check\s+(athletics|acrobatics|survival|intimidation|perception)\s+dc\s+(\d+)$`, `^perception\s+dc\s+(\d+)$`, `^start\s+combat$`, `^strike\s+(enemy_weak_melee|enemy_weak_ranged|pc_valeros_first_slice)\s+with\s+(longsword|shortbow)$`, `^move\s+(close|near|far|behind_cover)$`, and `^end\s+turn$`. Use state `character_sheet` when present; otherwise use `make_first_slice_character()`. Append all returned `CheckResult.model_dump()` values to `state["check_results"]`; when `CombatEngine.resolve_strike` returns a damage `RollResult`, append a compact string containing `damage_roll.roll_id` to `pending_narration` so the roll-log audit path can persist both attack and damage rolls until a dedicated damage result schema exists. Update `combat_state` with `.model_dump()` when combat changes. Catch `ValueError` from parser/rules/combat services and return `{"pending_narration": ["Rules error: {message}. Try `roll athletics dc 15`, `start combat`, or `strike enemy_weak_melee with longsword`."]}` with no new roll. Unsupported inputs return the same deterministic help message rather than `{}`. Addresses review concern: exact matching, silent failures, and uncaught validation errors. Do not call `services.llm`; do not parse DCs from narration prose; do not accept unsupported stats or position tags.</action>
  <verify>
    <automated>uv run pytest tests/agents/test_rules_lawyer_phase5.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/agents/rules_lawyer/node.py` contains `RulesEngine(` and `CombatEngine(`
    - `src/sagasmith/agents/rules_lawyer/node.py` does not contain `_TRIGGER_PHRASES`
    - `tests/agents/test_rules_lawyer_phase5.py` asserts unsupported input returns `pending_narration` containing `Rules error:` and no new `check_results`
    - `tests/agents/test_rules_lawyer_phase5.py` asserts `ROLL   ATHLETICS   DC   15` and `roll athletics dc 15` both resolve to one check result
    - `tests/agents/test_rules_lawyer_phase5.py` asserts invalid combat actions catch `ValueError` and return user-visible error state
    - `tests/agents/test_rules_lawyer_phase5.py` asserts `services.llm` is not used by passing an object that raises if touched
  </acceptance_criteria>
  <done>RulesLawyer resolves first-slice checks and combat from structured deterministic services, with unsupported input failing closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route combat and seed graph play state with the pregen sheet</name>
  <files>src/sagasmith/graph/routing.py, src/sagasmith/graph/state.py, src/sagasmith/tui/app.py, tests/graph/test_routing.py, tests/agents/test_rules_lawyer_phase5.py</files>
  <read_first>
    - src/sagasmith/graph/routing.py
    - src/sagasmith/graph/state.py
    - src/sagasmith/tui/app.py
    - src/sagasmith/rules/first_slice.py
    - tests/graph/test_routing.py
  </read_first>
  <behavior>
    - `route_by_phase({"phase": "combat"})` returns `rules_lawyer`, not `END`, while completed combat sets `phase="play"` before the next route.
    - `_build_play_state()` includes `character_sheet` equal to `make_first_slice_character().model_dump()` only when the incoming/current state has no sheet; it preserves an existing sheet so combat HP changes are not overwritten.
    - `SagaGraphState` already contains `check_results` and `combat_state`; tests assert this contract before node wiring.
    - Graph tests still pass for onboarding, play, paused, and session_end routing.
  </behavior>
  <action>Change `PHASE_TO_ENTRY[Phase.COMBAT.value]` in `src/sagasmith/graph/routing.py` from `END` to `"rules_lawyer"`. Update the module docstring to say Phase 5 routes combat through RulesLawyer first-slice mechanics. In `rules_lawyer_node`, when `CombatEngine.is_encounter_complete(updated_state)` is true, return `combat_state`, append a deterministic completion line to `pending_narration`, and set `phase` back to `"play"`. In `SagaSmithApp._build_play_state`, import `make_first_slice_character` locally inside the method and set `"character_sheet"` to the existing sheet if present; otherwise use `make_first_slice_character().model_dump()`. Preserve all existing state keys and the `current_turn_id` behavior. Add/keep tests that `SagaGraphState.__annotations__` contains both `check_results` and `combat_state`. Addresses review concerns: combat completion route was unowned and `_build_play_state` could overwrite live character state.</action>
  <verify>
    <automated>uv run pytest tests/graph/test_routing.py tests/agents/test_rules_lawyer_phase5.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/graph/routing.py` contains `Phase.COMBAT.value: "rules_lawyer"`
    - `src/sagasmith/tui/app.py` contains `make_first_slice_character().model_dump()` and preserves an existing `character_sheet`
    - `tests/graph/test_routing.py` asserts combat routes to `rules_lawyer`
    - `tests/agents/test_rules_lawyer_phase5.py` asserts completed combat returns `phase == "play"`
  </acceptance_criteria>
  <done>Graph/TUI state reaches deterministic mechanics with a valid pregen sheet and combat phase no longer terminates immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI player input → RulesLawyer parser | Free text could request unsupported mechanics or hidden math. |
| RulesLawyer → graph state | Returned dict mutates check_results, combat_state, and state_deltas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | `rules_lawyer_node` parser | mitigate | Use exact supported patterns and reject unsupported stats/actions before rolling. |
| T-05-08 | Elevation of Privilege | `services.llm` access | mitigate | Tests inject an LLM object that raises if touched; node must not call it. |
| T-05-09 | Information Disclosure | graph state sheet seeding | accept | Pregen PC sheet is player-visible; no enemy GM-only data is seeded until combat starts. |
</threat_model>

<verification>
Run `uv run pytest tests/agents/test_rules_lawyer_phase5.py tests/graph/test_routing.py -q` and `uv run pyright src/sagasmith/agents/rules_lawyer src/sagasmith/graph src/sagasmith/tui/app.py tests/agents tests/graph`.
</verification>

<success_criteria>
- RulesLawyer handles first-slice check/combat commands with deterministic services only.
- Combat phase routes to RulesLawyer.
- TUI-created graph state includes a valid first-slice pregen sheet.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rules-first-pf2e-vertical-slice/05-03-SUMMARY.md`
</output>
