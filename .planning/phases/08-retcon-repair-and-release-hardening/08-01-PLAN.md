---
phase: 08-retcon-repair-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - "07-06"
files_modified:
  - src/sagasmith/persistence/migrations/0008_retcon_audit.sql
  - src/sagasmith/schemas/persistence.py
  - src/sagasmith/persistence/repositories.py
  - src/sagasmith/agents/archivist/transcript_context.py
  - tests/persistence/test_retcon_repositories.py
autonomous: true
requirements:
  - QA-01
  - QA-02
must_haves:
  truths:
    - "Completed turns can be marked retconned while their raw rows remain available for audit/debug reads."
    - "Canonical persistence reads exclude retconned turns by default."
    - "Retcon eligibility can be computed from completed turns plus existing checkpoint refs."
  artifacts:
    - path: src/sagasmith/persistence/migrations/0008_retcon_audit.sql
      provides: "SQLite schema for retconned turn status, retcon audit records, and vault-write audit records"
    - path: src/sagasmith/schemas/persistence.py
      provides: "Typed RetconAuditRecord and VaultWriteAuditRecord schemas plus TurnRecord retconned status"
    - path: src/sagasmith/persistence/repositories.py
      provides: "Canonical turn/transcript helpers and retcon audit repositories"
  key_links:
    - from: src/sagasmith/agents/archivist/transcript_context.py
      to: src/sagasmith/persistence/repositories.py
      via: "canonical transcript query helper"
      pattern: "canonical.*transcript|include_retconned"
---

<objective>
Create the persistence contract for audit-retained retcons and canonical-only reads.

Purpose: Phase 8 retcon behavior must never delete trust records as its primary mechanism (D-02), and every canon-consuming subsystem needs shared query helpers so `status != retconned` filters do not drift (D-09, D-10).
Output: Migration 0008, typed persistence models, repository methods for recent eligible turns / canonical reads / retcon audits, and tests.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-retcon-repair-and-release-hardening/08-CONTEXT.md
@docs/specs/PERSISTENCE_SPEC.md
@docs/specs/STATE_SCHEMA.md
@.planning/phases/07-memory-vault-and-resume/07-02-SUMMARY.md
@.planning/phases/07-memory-vault-and-resume/07-03-SUMMARY.md

<interfaces>
Existing contracts to preserve:
```python
# src/sagasmith/schemas/persistence.py
class TurnRecord(SchemaModel):
    turn_id: str
    campaign_id: str
    session_id: str
    status: Literal["complete", "needs_vault_repair", "narrated", "discarded", "retried"]
    started_at: str
    completed_at: str
    schema_version: int = Field(ge=1)
    sync_warning: str | None = None

# src/sagasmith/persistence/repositories.py
class TurnRecordRepository:
    def upsert(self, record: TurnRecord) -> str: ...
    def get(self, turn_id: str) -> TurnRecord | None: ...

class TranscriptRepository:
    def append(self, entry: TranscriptEntry) -> int: ...
    def list_for_turn(self, turn_id: str) -> list[TranscriptEntry]: ...

class CheckpointRefRepository:
    def list_for_turn(self, turn_id: str) -> list[CheckpointRef]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retcon and vault-write audit schema</name>
  <files>src/sagasmith/persistence/migrations/0008_retcon_audit.sql, src/sagasmith/schemas/persistence.py, tests/persistence/test_retcon_repositories.py</files>
  <read_first>src/sagasmith/persistence/migrations/0006_turn_record_status.sql, src/sagasmith/persistence/migrations/0007_vault_sync_warning.sql, src/sagasmith/schemas/persistence.py, tests/persistence/test_migrations.py, tests/persistence/test_campaign_settings_schema.py</read_first>
  <behavior>
    - Test 1: `apply_migrations()` reaches schema version 8.
    - Test 2: `TurnRecord(status="retconned")` validates and can be written/read.
    - Test 3: `RetconAuditRecord` requires selected turn, affected turn ids, prior checkpoint id, token, timestamp, and reason.
    - Test 4: `VaultWriteAuditRecord` records turn_id + vault_path + operation + recorded_at.
  </behavior>
  <action>Implement D-02, D-09, and D-10 at the schema layer. Create migration `0008_retcon_audit.sql` that recreates `turn_records` with status CHECK values exactly `complete`, `needs_vault_repair`, `narrated`, `discarded`, `retried`, `retconned`; preserves `sync_warning`; creates `retcon_audit(retcon_id TEXT PRIMARY KEY, campaign_id TEXT NOT NULL, selected_turn_id TEXT NOT NULL, affected_turn_ids_json TEXT NOT NULL, prior_checkpoint_id TEXT NOT NULL, confirmation_token TEXT NOT NULL, reason TEXT NOT NULL, created_at TEXT NOT NULL)`; creates `vault_write_audit(id INTEGER PRIMARY KEY AUTOINCREMENT, turn_id TEXT NOT NULL, vault_path TEXT NOT NULL, operation TEXT NOT NULL, recorded_at TEXT NOT NULL)`. In `schemas/persistence.py`, extend `TurnRecord.status` with `retconned`; add `RetconAuditRecord` and `VaultWriteAuditRecord` SchemaModel classes with those fields. Do not add destructive-delete fields or cascade behavior.</action>
  <verify>
    <automated>uv run pytest tests/persistence/test_migrations.py tests/persistence/test_campaign_settings_schema.py tests/persistence/test_retcon_repositories.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/persistence/migrations/0008_retcon_audit.sql` contains `retconned`.
    - `src/sagasmith/persistence/migrations/0008_retcon_audit.sql` contains `CREATE TABLE IF NOT EXISTS retcon_audit`.
    - `src/sagasmith/persistence/migrations/0008_retcon_audit.sql` contains `CREATE TABLE IF NOT EXISTS vault_write_audit`.
    - `src/sagasmith/schemas/persistence.py` contains `class RetconAuditRecord`.
    - `src/sagasmith/schemas/persistence.py` contains `class VaultWriteAuditRecord`.
  </acceptance_criteria>
  <done>Retcon metadata can be migrated, validated, and represented without deleting original rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical query and eligibility repositories</name>
  <files>src/sagasmith/persistence/repositories.py, src/sagasmith/agents/archivist/transcript_context.py, tests/persistence/test_retcon_repositories.py</files>
  <read_first>src/sagasmith/persistence/repositories.py, src/sagasmith/agents/archivist/transcript_context.py, tests/agents/archivist/test_transcript_context.py, tests/persistence/test_repositories.py</read_first>
  <behavior>
    - Test 1: `TurnRecordRepository.list_recent_completed(campaign_id, limit=5)` returns only status `complete`, newest first.
    - Test 2: `TurnRecordRepository.list_affected_suffix(campaign_id, selected_turn_id)` returns selected completed turn and later completed turns, excluding already retconned rows.
    - Test 3: `TranscriptRepository.list_canonical_for_campaign(campaign_id, limit=8)` excludes transcript entries whose `turn_records.status = 'retconned'` unless `include_retconned=True`.
    - Test 4: `get_recent_transcript_context()` uses the canonical helper and no longer includes retconned transcript rows by default.
  </behavior>
  <action>Implement D-01, D-09, and D-10 through shared repository/query helpers. Add methods to `TurnRecordRepository`: `list_recent_completed(campaign_id: str, *, limit: int = 5) -> list[TurnRecord]`, `list_affected_suffix(campaign_id: str, selected_turn_id: str) -> list[TurnRecord]`, and `mark_retconned(turn_ids: list[str]) -> None`. Add `RetconAuditRepository.append/get` and `VaultWriteAuditRepository.append/list_for_turn/list_for_turns`. Add `TranscriptRepository.list_canonical_for_campaign(campaign_id: str, *, limit: int = 8, include_retconned: bool = False) -> list[TranscriptEntry]` using a JOIN to `turn_records`. Update `get_recent_transcript_context()` to call this helper so memory packets and `/recap` transcript slices exclude retconned turns by default per D-09. Keep explicit audit/debug access available only through `include_retconned=True` or turn-specific existing methods.</action>
  <verify>
    <automated>uv run pytest tests/persistence/test_retcon_repositories.py tests/agents/archivist/test_transcript_context.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/persistence/repositories.py` contains `def list_recent_completed`.
    - `src/sagasmith/persistence/repositories.py` contains `def list_affected_suffix`.
    - `src/sagasmith/persistence/repositories.py` contains `def list_canonical_for_campaign`.
    - `src/sagasmith/agents/archivist/transcript_context.py` contains `list_canonical_for_campaign`.
    - Tests prove a retconned turn transcript is absent from default memory context.
  </acceptance_criteria>
  <done>Retcon eligibility and canonical transcript access are centralized in repositories instead of scattered SQL filters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record vault writes during turn close for retcon impact summaries</name>
  <files>src/sagasmith/persistence/turn_close.py, src/sagasmith/persistence/repositories.py, tests/persistence/test_turn_close_vault.py, tests/persistence/test_retcon_repositories.py</files>
  <read_first>src/sagasmith/persistence/turn_close.py, src/sagasmith/persistence/repositories.py, tests/persistence/test_turn_close_vault.py</read_first>
  <behavior>
    - Test 1: When `close_turn()` writes vault pages, each relative path is appended to `vault_write_audit` for the turn.
    - Test 2: If the SQLite transaction fails, no vault-write audit rows are committed.
    - Test 3: If vault writing fails before a path is written, no false successful path is recorded.
  </behavior>
  <action>Implement D-07 and D-11 support data. In `close_turn()`, after each successful `vault_service.write_page(...)` call, append a `VaultWriteAuditRecord` with `operation='write_page'`, the current `turn_record.turn_id`, the relative path string, and UTC timestamp. Only record after the write succeeds. This gives `/retcon` an exact impact list for the confirmation screen and lets repair logic know which vault pages were affected. Do not alter the existing ordering: SQLite turn-close transaction commits before master vault writes; vault write audit rows may be committed after successful file writes because the audit reflects post-commit side effects.</action>
  <verify>
    <automated>uv run pytest tests/persistence/test_turn_close_vault.py tests/persistence/test_retcon_repositories.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/persistence/turn_close.py` contains `VaultWriteAuditRepository`.
    - `tests/persistence/test_turn_close_vault.py` asserts `vault_write_audit` row count for a successful vault write.
    - Existing `needs_vault_repair` behavior remains covered by tests.
  </acceptance_criteria>
  <done>Future retcon confirmation can report affected vault outputs from durable audit metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player-facing canon -> audit/debug rows | Retconned rows remain stored but must not flow back into canon by default. |
| SQLite repository -> memory/vault rebuild consumers | Shared helpers decide which turns count as canonical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `turn_records.status` | mitigate | Add explicit `retconned` status through migration CHECK and Pydantic Literal instead of ad hoc metadata. |
| T-08-02 | Information Disclosure | `get_recent_transcript_context` | mitigate | Route through `TranscriptRepository.list_canonical_for_campaign(... include_retconned=False)` so removed canon is not retrieved for future play. |
| T-08-03 | Repudiation | Retcon audit | mitigate | Store selected turn, affected suffix, prior checkpoint id, confirmation token, reason, and timestamp in `retcon_audit`. |
</threat_model>

<verification>
Run the targeted persistence tests, then `uv run ruff check src/sagasmith/persistence src/sagasmith/agents/archivist tests/persistence tests/agents/archivist`.
</verification>

<success_criteria>
- QA-01 groundwork exists: eligible completed turns can be listed and audit metadata can be stored.
- QA-02 groundwork exists: canonical transcript/memory reads exclude retconned turns by default.
- D-02/D-09/D-10 are implemented through durable schema and shared repository APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/08-retcon-repair-and-release-hardening/08-01-SUMMARY.md`
</output>
