---
phase: 08-retcon-repair-and-release-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - src/sagasmith/tui/commands/control.py
  - src/sagasmith/tui/app.py
  - tests/tui/test_retcon_command.py
autonomous: true
requirements:
  - QA-01
  - QA-02
must_haves:
  truths:
    - "`/retcon` shows recent eligible completed turns instead of silently targeting latest turn."
    - "The player must type an exact turn-specific confirmation token before rollback."
    - "Successful retcon returns the player to the prior safe prompt/checkpoint with a concise completion message."
    - "Blocked retcon explains repair guidance without partially mutating canon."
  artifacts:
    - path: src/sagasmith/tui/commands/control.py
      provides: "RetconCommand picker/preview/confirmation flow"
    - path: tests/tui/test_retcon_command.py
      provides: "Command-level tests for picker, token confirmation, success, and block paths"
  key_links:
    - from: src/sagasmith/tui/commands/control.py
      to: src/sagasmith/graph/runtime.py
      via: "app.graph_runtime.preview_retcon and confirm_retcon"
      pattern: "preview_retcon|confirm_retcon"
---

<objective>
Replace the Phase 8 `/retcon` stub with an explicit picker, preview, typed-confirmation, and runtime execution flow.

Purpose: D-01 through D-08 define retcon as a deliberate canon correction tool, not a quick undo. The TUI command must surface choice, impacts, and exact confirmation while delegating rollback to the runtime service.
Output: Working `/retcon` command flow and TUI command tests.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-retcon-repair-and-release-hardening/08-CONTEXT.md
@docs/specs/GAME_SPEC.md
@docs/specs/PERSISTENCE_SPEC.md
@.planning/phases/08-retcon-repair-and-release-hardening/08-02-SUMMARY.md

<interfaces>
Existing TUI command pattern:
```python
# src/sagasmith/tui/commands/control.py
@dataclass(frozen=True)
class RetconCommand:
    name: str = "retcon"
    description: str = "Retcon the last completed turn (confirmation required)."
    def handle(self, app: SagaSmithApp, args: tuple[str, ...]) -> None: ...

def _write(app: SagaSmithApp, line: str) -> None: ...

# Expected from Plan 08-02
app.graph_runtime.preview_retcon(selected_turn_id: str | None = None)
app.graph_runtime.confirm_retcon(selected_turn_id: str, confirmation_token: str)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `/retcon` recent-turn picker and preview rendering</name>
  <files>src/sagasmith/tui/commands/control.py, tests/tui/test_retcon_command.py</files>
  <read_first>src/sagasmith/tui/commands/control.py, src/sagasmith/tui/commands/registry.py, src/sagasmith/tui/widgets/narration.py, tests/cli/test_play_cmd.py</read_first>
  <behavior>
    - Test 1: `/retcon` with no args prints a numbered list of recent eligible candidates returned by `graph_runtime.preview_retcon/list_candidates` and does not call confirm.
    - Test 2: `/retcon turn_000003` prints a concise summary and effects list including state rewind, transcript/mechanics/vault/memory effects, audit retention, and canonical exclusion.
    - Test 3: If no graph runtime is active, command prints `[system] /retcon unavailable: no active graph runtime.`
    - Test 4: A RetconBlockedError preview prints `blocked` and its repair guidance.
  </behavior>
  <action>Implement D-01, D-05, and D-07. Replace the stub in `RetconCommand.handle`. Supported forms: `/retcon` lists candidates; `/retcon <turn_id>` displays preview; `/retcon <turn_id> <confirmation tokens...>` attempts confirmation. Candidate list lines must use deterministic text: `[system] /retcon candidates:` followed by `1. {turn_id} — {completed_at} — {summary}`. Preview must include `[system] /retcon preview for {turn_id}:`, `Affected turns: ...`, `Vault outputs: ...`, `Effects: state rewind; transcript/mechanics/vault/memory canonical exclusion; audit retained`, and `Type: /retcon {turn_id} RETCON {turn_id}`. Do not silently target latest turn; no args only lists choices.</action>
  <verify>
    <automated>uv run pytest tests/tui/test_retcon_command.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/tui/commands/control.py` contains `/retcon candidates:`.
    - `src/sagasmith/tui/commands/control.py` contains `Type: /retcon {turn_id} RETCON {turn_id}` or equivalent formatted output.
    - Tests assert no-arg `/retcon` does not call `confirm_retcon`.
  </acceptance_criteria>
  <done>The user can inspect eligible turns and exact retcon effects before confirming.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed confirmation and completion/block messaging</name>
  <files>src/sagasmith/tui/commands/control.py, src/sagasmith/tui/app.py, tests/tui/test_retcon_command.py</files>
  <read_first>src/sagasmith/tui/commands/control.py, src/sagasmith/tui/app.py, tests/tui/test_retcon_command.py, tests/integration/test_narration_recovery.py</read_first>
  <behavior>
    - Test 1: `/retcon turn_000003 RETCON turn_000003` calls `confirm_retcon` with selected turn id and exact token.
    - Test 2: Success prints `[system] Retcon complete: returned to checkpoint before turn_000003. Affected turns are audit-retained and excluded from canon.`
    - Test 3: Wrong token prints blocked guidance and does not append removed canon details.
    - Test 4: After success, the app syncs graph narration/status from the rewound runtime without exiting.
  </behavior>
  <action>Implement D-06, D-08, and D-12. Parse confirmation by joining args after the first turn id with spaces; expected token is exactly `RETCON {turn_id}`. Call `app.graph_runtime.confirm_retcon(selected_turn_id=turn_id, confirmation_token=token)`. On success, write the completion message using only turn ids/counts, never transcript excerpts or vault body text. If `SagaSmithApp` needs a small helper to resync after retcon, add `sync_after_retcon()` that calls existing `_sync_narration_from_graph()` and `_sync_mechanics_from_graph()` inside suppress blocks; do not exit the app. Catch RetconBlockedError by printing `[system] /retcon blocked: {message}` and `[system] repair: {repair_guidance}`.</action>
  <verify>
    <automated>uv run pytest tests/tui/test_retcon_command.py tests/integration/test_narration_recovery.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/tui/commands/control.py` contains `confirm_retcon`.
    - `src/sagasmith/tui/commands/control.py` contains `Retcon complete`.
    - `src/sagasmith/tui/commands/control.py` contains `/retcon blocked`.
    - No success or blocked message includes transcript content from a removed turn in tests.
  </acceptance_criteria>
  <done>The TUI requires turn-specific typed confirmation and returns to play after successful rollback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player text command -> retcon confirmation | Untrusted text authorizes canon exclusion. |
| Runtime retcon result -> player-facing TUI | Could leak removed canon if rendered carelessly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Spoofing | `/retcon` confirmation | mitigate | Require exact turn-specific phrase `RETCON {turn_id}` parsed from the command. |
| T-08-09 | Repudiation | Retcon UX | mitigate | Preview shows affected turn ids/effects before confirmation; Plan 08-02 audit stores confirmation token. |
| T-08-10 | Information Disclosure | Completion/block messages | mitigate | Messages reference only turn ids/counts and repair guidance, never removed transcript/vault body details. |
</threat_model>

<verification>
Run `uv run pytest tests/tui/test_retcon_command.py tests/integration/test_retcon_runtime.py -x`.
</verification>

<success_criteria>
- QA-01: Player can select and confirm a completed-turn retcon from the TUI.
- QA-02: TUI messaging explains canonical exclusion without exposing removed canon details.
- D-01 through D-08 are represented in the command flow.
</success_criteria>

<output>
After completion, create `.planning/phases/08-retcon-repair-and-release-hardening/08-03-SUMMARY.md`
</output>
