---
phase: 08-retcon-repair-and-release-hardening
plan: 04
type: execute
wave: 1
depends_on:
  - "07-06"
files_modified:
  - src/sagasmith/evals/harness.py
  - src/sagasmith/cli/smoke_cmd.py
  - tests/evals/test_mvp_smoke.py
  - tests/evals/test_smoke_cli.py
  - Makefile
autonomous: true
requirements:
  - QA-08
  - QA-09
must_haves:
  truths:
    - "Developer can run a no-paid-call MVP smoke suite covering install/entrypoint, init, configure, onboard, play skill challenge, play simple combat, quit, and resume."
    - "At least one shell-level `uv run sagasmith ...` smoke path proves the local CLI entrypoint."
    - "`make release-gate` blocks release unless lint, format check, type check, tests, MVP smoke, and secret scan pass."
  artifacts:
    - path: src/sagasmith/evals/harness.py
      provides: "MVP no-paid-call smoke checks with observable step names"
    - path: src/sagasmith/cli/smoke_cmd.py
      provides: "CLI smoke mode for MVP smoke"
    - path: Makefile
      provides: "release-gate wrapper command"
  key_links:
    - from: Makefile
      to: src/sagasmith/cli/smoke_cmd.py
      via: "`make release-gate` invokes `uv run sagasmith smoke --mode mvp`"
      pattern: "release-gate.*smoke"
---

<objective>
Add the MVP no-paid-call smoke suite and release-blocking quality gate.

Purpose: QA-08 and QA-09 require proving the local MVP path and blocking release unless lint, formatting, type checking, tests, smoke, and secret scanning pass. D-13 through D-16 lock the exact smoke/release expectations.
Output: Extended smoke harness/CLI tests and `make release-gate`.
</objective>

<execution_context>
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/workflows/execute-plan.md
@C:/PersonalDevFiles/KiloCodeFiles/AI-TTRPG-GoAnywhereDoAnything/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-retcon-repair-and-release-hardening/08-CONTEXT.md
@docs/specs/GAME_SPEC.md
@docs/specs/PF2E_MVP_SUBSET.md
@docs/specs/LLM_PROVIDER_SPEC.md
@.planning/phases/07-memory-vault-and-resume/07-05-SUMMARY.md
@.planning/phases/07-memory-vault-and-resume/07-06-SUMMARY.md

<interfaces>
Existing smoke contracts:
```python
# src/sagasmith/evals/harness.py
@dataclass(frozen=True)
class SmokeCheck:
    name: str
    ok: bool
    detail: str = ""

@dataclass
class SmokeResult:
    checks: list[SmokeCheck]
    @property
    def ok(self) -> bool: ...
    def format(self) -> str: ...

def run_smoke() -> SmokeResult: ...

# src/sagasmith/cli/smoke_cmd.py
class SmokeMode(StrEnum):
    FAST = "fast"
    PYTEST = "pytest"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add layered MVP smoke harness checks</name>
  <files>src/sagasmith/evals/harness.py, tests/evals/test_mvp_smoke.py</files>
  <read_first>src/sagasmith/evals/harness.py, src/sagasmith/app/campaign.py, src/sagasmith/cli/configure_cmd.py, src/sagasmith/cli/onboard_cmd.py, src/sagasmith/cli/play_cmd.py, tests/integration/test_rules_first_vertical_slice.py</read_first>
  <behavior>
    - Test 1: `run_mvp_smoke()` returns named OK checks for `mvp.install_entrypoint`, `mvp.init`, `mvp.configure_fake`, `mvp.onboard`, `mvp.play_skill_challenge`, `mvp.play_simple_combat`, `mvp.quit`, and `mvp.resume`.
    - Test 2: MVP smoke uses fake/no provider path only and does not require OpenRouter credentials.
    - Test 3: MVP smoke failure includes the step name and non-secret detail.
  </behavior>
  <action>Implement D-13 and D-14. Add `run_mvp_smoke() -> SmokeResult` beside `run_smoke()`. Use existing local in-process APIs and deterministic fake services to exercise: import/entrypoint availability (`sagasmith.__version__` plus Typer app import), campaign init in TemporaryDirectory, fake configure/settings write, onboarding store commit or existing onboarding command helper with deterministic fixture, rules-first skill challenge via `RulesEngine`, simple combat via `CombatEngine`, quit/session-end persistence path from Phase 7, and later-process resume by closing/reopening the campaign DB/app and loading scrollback/memory state. Each check name must use the exact names listed in behavior. Do not make network calls or require real API keys.</action>
  <verify>
    <automated>uv run pytest tests/evals/test_mvp_smoke.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/evals/harness.py` contains `def run_mvp_smoke`.
    - `tests/evals/test_mvp_smoke.py` asserts all eight exact check names are present.
    - MVP smoke test fixtures contain no real secret-shaped API key outside existing RedactionCanary synthetic fixtures.
  </acceptance_criteria>
  <done>The no-paid-call MVP path is observable through an in-process smoke harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI MVP smoke mode and shell-level entrypoint proof</name>
  <files>src/sagasmith/cli/smoke_cmd.py, tests/evals/test_smoke_cli.py</files>
  <read_first>src/sagasmith/cli/smoke_cmd.py, src/sagasmith/cli/main.py, tests/evals/test_smoke_cli.py</read_first>
  <behavior>
    - Test 1: `uv run sagasmith smoke --mode mvp` exits 0 when MVP smoke checks pass.
    - Test 2: CLI output contains `OK  mvp.init` and `OK  mvp.resume`.
    - Test 3: `SmokeMode` includes `MVP = "mvp"`.
  </behavior>
  <action>Implement D-14 shell-level coverage. Extend `SmokeMode` with `MVP = "mvp"`. In `smoke()`, when mode is MVP, call `run_mvp_smoke()`, echo formatted output, and exit 1 if `result.ok` is false. Add a test that invokes the local CLI entrypoint through subprocess command `uv run sagasmith smoke --mode mvp` from the repository root. Keep the existing `fast` and `pytest` modes unchanged.</action>
  <verify>
    <automated>uv run pytest tests/evals/test_smoke_cli.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sagasmith/cli/smoke_cmd.py` contains `MVP = "mvp"`.
    - `tests/evals/test_smoke_cli.py` contains `uv run sagasmith smoke --mode mvp`.
    - CLI test asserts output includes `mvp.resume`.
  </acceptance_criteria>
  <done>Developer can prove local entrypoint/install behavior through a shell-level MVP smoke command.</done>
</task>

<task type="auto">
  <name>Task 3: Add release-gate Make target with secret scanning</name>
  <files>Makefile, tests/evals/test_mvp_smoke.py</files>
  <read_first>Makefile, .pre-commit-config.yaml, .gitleaks.toml, tests/evals/test_mvp_smoke.py</read_first>
  <action>Implement D-15 and D-16. Update `.PHONY` to include `release-gate` and `secret-scan`. Add `secret-scan:` target that runs `uv run gitleaks detect --source . --config .gitleaks.toml --no-banner` if the gitleaks executable is available through uv/pre-commit; if project convention prefers pre-commit, use `uv run pre-commit run gitleaks --all-files`. Add `release-gate:` target that runs these commands in order: `$(MAKE) lint`, `$(MAKE) format-check`, `$(MAKE) typecheck`, `$(MAKE) test`, `uv run sagasmith smoke --mode mvp`, and `$(MAKE) secret-scan`. Do not replace existing `smoke` or `precommit` targets.</action>
  <verify>
    <automated>uv run pytest tests/evals/test_mvp_smoke.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` contains `.PHONY:` line with `release-gate`.
    - `Makefile` contains `secret-scan:`.
    - `Makefile` contains `uv run sagasmith smoke --mode mvp`.
    - `Makefile` contains `release-gate:` after `secret-scan:` or in the same file.
  </acceptance_criteria>
  <done>`make release-gate` blocks release on lint, format, typecheck, tests, MVP smoke, and secret scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke fixtures -> CLI/test output | Fixture data and failure output must not contain provider secrets. |
| Release gate -> developer machine | Release command runs local tools and should fail closed on quality/security failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information Disclosure | MVP smoke output | mitigate | Use fake provider/settings and assert output details are non-secret; release gate includes secret scan. |
| T-08-12 | Repudiation | Release readiness | mitigate | `make release-gate` provides one auditable command wrapping lint, format, typecheck, tests, smoke, and gitleaks/pre-commit secret scan. |
| T-08-13 | Denial of Service | Shell-level smoke | accept | Smoke uses TemporaryDirectory and fake provider; no external network dependency. |
</threat_model>

<verification>
Run `uv run pytest tests/evals/test_mvp_smoke.py tests/evals/test_smoke_cli.py -x`, then run `make release-gate` before final phase verification.
</verification>

<success_criteria>
- QA-08: MVP no-paid-call smoke covers install/entrypoint, init, configure, onboard, play skill challenge, play simple combat, quit, and resume.
- QA-09: Release gate requires lint, type check, unit tests, smoke tests, and secret scan.
- D-13 through D-16 are implemented exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/08-retcon-repair-and-release-hardening/08-04-SUMMARY.md`
</output>
